node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local Netlify folder
.netlify

# MediaPipe wasm copied from node_modules at build (kept in lockstep with the JS)
public/mediapipe/

# Phase 6 keepsake-film: local render output + container render toolchain
.hf-bin/
hyperframes/keepsake-film/renders/
hyperframes/keepsake-film/fixtures/*-vars.json

# Video studio: local render output (finished web assets are copied to
# src/assets/landing/ and committed there instead) + snapshot QA scratch
hyperframes/studio/renders/
hyperframes/studio/*/snapshots/

# HyperFrames authoring skills (dev tooling; reinstall via: npx skills add heygen-com/hyperframes)
.agents/
.claude/skills/
skills-lock.json
scratch-shots/
