# STATE

## Goal
Rebuild the event studio (/host/events/:id) into one unified premium editor: single camera, in-canvas 2D·3D·Preview switching, DnD assets dock, AR occlusion + head-size calibration, AI Scene Director hero feature (user-approved plan).

## Now
W4 (mixed scenes) COMPLETE: 8 local commits ahead of origin/30501fe (camera-consistency fix, W4-A state core, W4-B mapping+booth, W4-C studio UI, logic-audit fixes, UX-review fixes, 2 docs). All gates green (tsc 0 · 337 tests · build ✓ · E2E mixed-scene harness). AWAITING USER PUSH AUTHORIZATION, then PR #14 description refresh.

## Studio v2 landed (all reviewed by me, per-wave commits)
- W1-A state core (146 lib tests): objects[]/selectedId model, add-vs-replace rule, withHistory undo/redo (coalescing, cap 50), draftMapping layers↔objects with layer-0 legacy mirror (single plain object saves byte-identical). f202260.
- W1-B booth: StageCanvas overlays[] + Overlay3D pieces[] (additive, legacy path byte-identical, one occluder max) + Booth config.layers wiring + pure animation.ts (float/pulse/spin 2D+3D). b43b3a2/a9b3904/872ddeb.
- W1-C: snap.ts (centre/thirds guides + nudge) + assetSources.ts (DockItem from uploads/experiences + filter). 0bf5054/aa9d4ae.
- W2-D1 studio UI: layers panel (select/reorder/delete/anim badges), undo/redo UI + Ctrl+Z/Y + Delete + arrow-nudge, snap guide lines, animation chips, Duplicate, multi-render 2D stage + both 3D views, Preview via overlays/pieces, N-blob save map. f202260.
- W2-D2: AssetsDock Library/Uploads/Mine tabs + search; TestOnPhone QR (experience/:id deep link, save-first flow); Library Duplicate pre-existed. dfb4c61.
- Gates at dfb4c61: tsc 0 · 309 tests · build ✓ · harness green all viewports.

## Verification harness (NEW)
- Playwright devDep + Chromium at /opt/pw-browsers/chromium-1194/chrome-linux/chrome (pass executablePath).
- DEV-only route /dev/studio (src/dev/StudioHarness.tsx) renders StudioShell via EventProvider slug="hope-gala" (code event, no Supabase auth). Dynamic-imported → tree-shaken from prod (verified absent).
- scripts/shoot-studio.mjs screenshots phone/tablet/desktop + asserts panel reachability. Run: `node scripts/shoot-studio.mjs http://localhost:5173 <tag>`.
- .env.local (gitignored, dummy Supabase creds, NO VITE_EVENT) required for dev runtime render — the app throws "supabaseUrl is required" without it.

## Was
P5: (1) platformGuide.ts — add a Studio + Scene Director section so the copilot answers studio questions (pure client, no deploy). (2) Polish sweep: grep-gate zero `noir-|gold-|ivory|champagne` classes and zero emoji glyphs in studio files (Creator2D/3D + creator3d/* are being DELETED, which removes the emoji + gold classes; verify none linger in NEW studio files). (3) Delete Creator2D.tsx, Creator3D.tsx, creator3d/{ModelCanvas,LiveCanvas}.tsx (+ REFERENCE SWEEP for stale imports; AnchorPanel/PropertiesPanel now unused too — check). (4) Add strategic tooltips where missing. (5) Final checkpoint, push -u origin claude/studio-editing-ux-improvements-xfn36m, draft PR.

## Next
1. P0 (above). Checkpoint: lint + test + build.
2. P1: FaceOccluder (canonical mesh + cranium, colorWrite:false renderOrder:-1) + FaceRig occlude/headScale props + db.ts getStudioSettings/setStudioSettings (app_settings key 'studio') + Booth.tsx~525/Overlay3D wiring + enable in creator3d/LiveCanvas. Checkpoint.
3. P2: unified StudioShell/StudioStage/Studio3DView/StudioPreview/PropertiesDock at route `studio`; ONE useCameraStream, video id studio-video; segmented 2D·3D·Preview switcher; creator/creator3d (+/admin) → query-preserving redirects; headScale slider. Checkpoint.
4. P3: AssetsDock (Bucket/Built-ins/Head pieces, click-to-add + pointer DnD w/ DragGhost, anchor snap); Higgsfield generate_3d bust → remote-assets → ReferenceBust.tsx; restyle Assets.tsx. Checkpoint.
5. P4: Scene Director — additive mode:'scene' in supabase/functions/ai-event-designer (deploy via Supabase MCP + commit identical code), sceneDirector.ts normalize/clamp, SceneDirectorPanel (3 cards, per-card credits, Accept/Skip/Regenerate, honest ai_key_invalid), ?scene= prefill. Checkpoint.
6. P5: platformGuide.ts studio docs (copilot Q&A); polish sweep — grep gates zero noir-|gold-|ivory|champagne + zero emoji in studio files; tooltips (mode switcher, anchor dots, calibration, credit buttons, pause); delete Creator2D/Creator3D/creator3d{ModelCanvas,LiveCanvas} + stale imports. Final checkpoint, commit, push -u origin claude/studio-editing-ux-improvements-xfn36m, draft PR.

## Constraints
- User (2026-07-07): Gemini API key must NEVER be committed to the repo; Supabase edge-function secrets only.
- (platform) Never break legacy-events sites — faceRig/Booth/FaceRig are shared; keep default behavior compatible (occlusion defaults must not alter legacy rendering unless enabled).
- (platform) No `.env.local` with VITE_EVENT during tests.
- (platform) No new npm dependencies; edge fn dir needs its own deno.json; never loosen tenant RLS.
- (plan, user-approved) Do NOT touch: StageCanvas.drawFrame semantics/Transform2D meaning, booth capture/submitPost, RLS/migrations, experiences schema (new persistence = jsonb config + app_settings 'studio' key only).
- (task) Push only to claude/studio-editing-ux-improvements-xfn36m; draft PR after push.
- User (2026-07-10, verbatim): "ensure they're ketp the same and consistent" — 2D-mode editor camera vs preview camera (2D "a bit zoomed and distored").
- User (2026-07-10): "an experience doesn't have to be just 2D or 3D... It's both." "there should only be one frame, but there can be multiple stickers. There's no limit on that. 3D models can also be multiple... that should be the default behavior." "It should persist whether it's 2D or 3D" (no deletion when switching modes).

## Decisions
- DECISION (user, 2026-07-10, W6): MESHY_API_KEY secret is SET in Supabase (user confirmed). Scene Director FOLDS INTO the Platform Copilot — one assistant, studio-aware (Scene-Director-first persona in the studio), docked RIGHT-SIDE panel (not modal) so the stage stays visible; accepted assets dispatch INTO the open draft (watch them appear in canvas + Scene Layers); composer cards per asset (transparency checkerboard for 2D, interactive R3F viewer for 3D models, Meshy progress % + rotating status verbs, Approve/Reject); accept-all → one composite. ALSO approved: scene templates (save-as-template + gallery) and booth reveal animation on scene apply.
- DECISION (user, 2026-07-10, W5): Meshy ONLY for runtime 3D gen (skip Higgsfield built-in pack); Gemini concept image → ai-generate-3d mode:'image' for Scene Director head pieces. MESHY_API_KEY → Supabase edge secrets (user sets).
- DECISION (user, 2026-07-10, W5): frame transparency = BROWSER-side chroma-key (Gemini prompts solid green bg; pure pixel fn keys out + pads to transparent 1080×1920 portrait; no new npm deps).
- DECISION (user, 2026-07-10, W5 scope): studio-only header (no event tabs); right panel = Scene Layers FIRST + "Editing — <name>" properties header; "Scene Type" label for the 4 catalog tabs; visual square asset tiles + upload thumbnails (auto GLB snapshot); name prompt on create + center-header name w/ pencil.
- DECISION (user, 2026-07-10): MIXED SCENES are the default model — one experience = ≤1 frame + N stickers + N 3D pieces + ONE optional scene-level filter slot (user picked "one filter slot per scene"); soft cap 20 objects (stickers+3D, frame exempt) with a counter (user picked over unlimited); scene persists across 2D/3D view switches (switching never deletes). Saved kind: 'composite' when mixed; single-family scenes keep today's kinds (back-compat); filter slot rides config.ambientShader; filter-only scene stays kind 'shader'.
- DECISION (2026-07-10): ShaderRunner.aspectCropped — cover-crop any aspect-mismatched source before texture upload (pure coverCropRect + tests); fixes distorted 2D live-filter view; equal-aspect sources pass through (capture dissolve unchanged).
- DECISION (user, 2026-07-09): multi-object = "multiple within a mode" (option B) + ship undo/delete; assets panel = uploads + my experiences (option B); implement ALL 4 extras; orchestrate via Sonnet/Opus subagents with UI/UX reviewer + assumptions auditor, me directing/reviewing.
- DECISION: layers contract — config.layers[] = full ordered object list, layer 0 mirrored into legacy singular fields; booth renders layers when present else legacy path; scene never mixes 2D and 3D. Shader stays single per experience.
- REVERSING DECISION 'never touch StageCanvas.drawFrame semantics' because the user explicitly requested multiple 2D objects — change is additive only (optional overlays[] prop; single-overlay path unchanged; legacy events safe).
- DECISION: 3D multi-object = one FaceRig per object (tracking core untouched; detection is globally throttled/shared so N rigs is cheap).
- DECISION: unified StudioShell replaces Creator2D/Creator3D tabs — user picked via AskUserQuestion (vs quick-switcher).
- DECISION: occluder from vendored MediaPipe canonical_face_model.obj (metric cm = faceRig.ts anchor space) + procedural cranium ellipsoid; meshBasicMaterial colorWrite:false, depthWrite true, renderOrder −1, raycast no-op → works in booth composite with zero StageCanvas changes.
- DECISION: headScale per-event app_settings key 'studio' {headScale} clamp 0.85–1.3; per-experience opt-out config.occlusion===false.
- DECISION: Scene Director = additive mode:'scene' in ai-event-designer + client orchestration over existing generateImage (1cr) / createExperience (free) / generate3d+pollJob (10cr); credits only on per-card Accept.
- DECISION: hand-rolled pointer-event DnD (no dnd-kit) — portal DragGhost + setPointerCapture; pure drop math in lib/studio/dnd.ts.
- DECISION: Higgsfield bust GLB vendored via scripts/remote-assets.json (fetch-remote-assets.yml Action has egress; sandbox does not); procedural ReferenceHead stays as fallback.

## Facts
- Branch: claude/studio-editing-ux-improvements-xfn36m. Commands: npm run lint (tsc --noEmit) · npm test (vitest run, node env, src/**/*.test.ts only, never .tsx) · npm run build · dev :5180.
- tsconfig excludes supabase/ — edge fns not type-checked by lint.
- Double-camera bug: src/components/admin/Creator2D.tsx:372 shader canvas `opacity: kind==='shader' ? 1 : 0.35` over raw video; shader RAF always on (532-553).
- Camera hook: src/components/booth/useCameraStream.ts:31 `useCameraStream(enabled=true, withAudio=false)`; booth video id booth-video; Overlay3D wired at Booth.tsx~525-534.
- Head space: src/lib/faceRig.ts ANCHOR_PRESETS in cm (crown y+8.3, ears ±7.7, chin −9.4, noseTip z+7.6); RIG_CAMERA origin fov 63; canonical bbox expectation x±7.7 y −9.4..+8.3 z ≤+7.6.
- Persistence: src/lib/db.ts experiences config jsonb (2D {transform,opacity}; 3D {anchor, procedural?}); getSetting/setSetting ~532-544; uploadAsset→assets bucket. Save shapes: Creator2D handleSave 652-716, Creator3D 227-271. Deep links ?id= from Library.tsx:184/195.
- Vendoring: scripts/remote-assets.json + .github/workflows/fetch-remote-assets.yml. Canonical OBJ source: https://raw.githubusercontent.com/google-ai-edge/mediapipe/master/mediapipe/modules/face_geometry/data/canonical_face_model.obj
- Design tokens: src/index.css @theme 11-68 / utilities 91-216 (liquid-glass = premium standard, --accent-rgb); lucide-react + BeamIcons gradient idiom; motion/react; zustand; react-resizable-panels. NO Tooltip primitive yet; legacy noir/gold classes still live in studio files.
- GEMINI_API_KEY secret is REJECTED by Google in prod (400 API_KEY_INVALID → app reports ai_key_invalid truthfully). Scene Director must degrade honestly. Sandbox cannot set secrets or reach *.supabase.co.
- GEMINI TRAP: ARRAY-of-OBJECT responseSchema hangs gemini-2.5-flash constrained decoding — encode arrays as JSON STRING fields (actionsJson pattern) in mode:'scene' schema.
- Plan file: /root/.claude/plans/i-want-to-improve-cozy-valley.md (user-approved).
- Creator2D.tsx map: Panel shims 16-41 · GoldSlider 92-115 · MagicGenerate 123-221 · LivePreview 237-431 (shader canvas opacity bug :372) · main 449 · camera 494-529 · shader RAF 532-553 · load edit 556-584 · builtin sync 587-598 · handleKindChange 607-628 · handleSave 652-716 · top bar 741-797 · kind selector 811-825 · shader list 829-853 · border list 856-902 · sticker list 905-947 · thumb 949-976 · MagicGenerate mount 983-1004 · LivePreview mount 1021-1029 · right panel 1038-1171.
- Creator3D.tsx map: Panel shims 24-52 · state 108-136 · load edit 139-164 · handleUpload 167-182 · handleSelectPreset 185-198 · handleTransformChange 208-210 · handleAnchorSelect 213-224 (same-anchor no reset; new → reset offset/rot keep scale) · handleSave 227-271 · top bar 298-389 (mode toggle 326-350, pause 353-368) · AnchorPanel mount 399-408 · AiGeneratePanel 410-414 (navigates `${base}/creator3d?id=`) · canvases 451-470 · caption 474-502 · PropertiesPanel 511-526.
- shaders.ts: ShaderParam{key,label,min,max,step,default} :15 · ShaderDef :24 · SHADERS :67 · SHADER_MAP :565 · FILTER_SHADERS :568 (excl none+special) · defaultParams :570 · class ShaderRunner :584.
- StageCanvas drawFrame steps: 1 video mirror · 2 shader coverFit · 3 three canvas by container id `#<threeId> canvas` drawImage · 4 overlay: cx=w/2+(t.x/100)*w, rotate deg, scale, drawImage(img,-w/2,-h/2,w,h) · 5 sparkles · 6 signature. PREVIEW canvas set at :328.
- borders.ts pure (encodeURIComponent): BUILTIN_BORDERS :294, toDataUrl :46, BuiltinBorder{id,name,kind:'border'|'2d_filter',svg}.
- cn() at src/lib/cn.ts. canonical_face_model.obj downloaded to src/assets/ar/ (468v/898f, bbox x±7.74 y −9.40..8.26 z −2.44..7.59, faces `f v/vt`).
- ShaderRunner.draw signature (Creator2D:539): runner.draw(video, shaderId, params, mirror?) — verify exact param list at shaders.ts:584 before StudioStage.

## Done
- W4 (2026-07-10) — RESULT: MIXED SCENES shipped end-to-end. One experience = ≤1 frame + N stickers + N 3D pieces (soft cap 20, frame exempt) + one filter slot (config.ambientShader); kind 'composite' when mixed; 2D/3D/Preview are pure views (content persists); booth applies a composite as one unit (frame+3D+filter slots, layers split by kind, ambient released on switch-away); clicks ALWAYS append for stickers/3D (appendObject — browse-swap removed after UI/UX review flagged silent replace; frames swap via placeFrame); hidden persists on layers and renders nowhere (booth skips; Figma model); grouped layers panel (Frame/Stickers/3D) + eyes + N/20 chip + duplicate-name numbering; orbit camera reframed for crown headroom (z46/target-3, screenshot-verified); tooltip offset + top scrim; dock tabs echo stage view, default Filter; anchor list self-explains when nothing selected. Also: ShaderRunner.aspectCropped fixes the distorted 2D live-filter view (pixel probe: centered square ratio 1.0). Reviewers: logic auditor (0 CRITICAL; H1/M2/L3/L6/L9/L10 fixed) + Playwright UI/UX reviewer (2 HIGH fixed + 4 MED fixed). Gates: tsc 0 · 337 tests · build ✓. Deferred: ambient params id-only in booth (pre-existing pattern), cross-group reorder motion (cosmetic), composite-without-mirror latent coupling (Booth.tsx is3D needs config.anchor — not producible via studio), picker cosmetics (3D section header gate PickerDrawer.tsx:325, doubled composite badge :197-208), phantom placeholder sphere in empty live 3D (needs live hardware), Scene Director emitting ONE composite instead of 3 pieces (natural follow-up).
- W3 (2026-07-10) — RESULT: e7e97dc. (1) AR-NOT-TRACKING root cause: studio live 3D never called initializeFaceLandmarker (LiveCanvas, the old caller, was deleted) → FaceRig now SELF-INITS (useEffect, idempotent); + faceTracking never-init warn + isFaceLandmarkerReady + TrackerLoadingPill. VERIFIED: scripts/check-ar-tracking.mjs → "WASM requests: vision_wasm_internal.js,vision_wasm_internal.wasm". (2) BLACK-APP root cause: drei <Text> in AnchorDots suspends on CDN font fetch → suspended the ROUTE Suspense (App.tsx:149 fallback null) → whole app display:none. Fixed: Suspense around <Text> + in-canvas Suspense both Studio3DView canvases + NEW ui/ErrorBoundary around stage 3D/preview (UI/UX HIGH #3). (3) UI/UX HIGH #1: addOrReplaceObject → browse-swap (untouched same-kind selection swaps in place, incl. subkind check) vs committed-add (touched → append); isUntouched = default transform/anchorConfig+animation none. 5 new tests; E2E: crown→tiara swap=1 layer, tiara moved to chin→crown click=2 layers. (4) HIGH #2: preview caption → top slot (under pill); overlap=false @900/1440. (5) W3 auditor fixes: TestOnPhone Hidden-piece branch, AssetsDock kind-tab lock, DnD MAX_OBJECTS cap via draftRef. Gates: tsc 0 · 314 tests · build ✓ · harness green. NOTE sandbox blocks landmarker MODEL fetch — pill stays up there; real tracking needs user hardware.
- Exploration + design (2026-07-09) — RESULT: full studio map (2 explore agents), double-camera root cause CONFIRMED at Creator2D.tsx:372, plan designed (Plan agent) + user approved; 3 decisions locked via AskUserQuestion (unified editor / canonical mesh + Higgsfield bust / AI Scene Director).
- P0 (2026-07-09) — RESULT: src/lib/studio/{state,draftMapping,dnd,occluder,sceneDirector}.ts + colocated tests (61 new, all green); ui/Tooltip.tsx (portal, motion, delay); Creator2D double-camera fix (shader canvas opacity 0 + RAF `if(kind!=='shader')return` outside shader mode); canonical_face_model.obj vendored to src/assets/ar/ (468v, bbox verified x±7.74/y−9.4..8.26/z−2.44..7.59) + remote-assets.json entry; types.ts ExperienceConfig +scene/+occlusion. CHECKPOINT: lint clean · 224 tests (was 163) · build ✓.
- PANEL FIX + AUDIT (2026-07-09) — RESULT: USER-REPORTED BUG: AssetsDock (hidden md:block) + PropertiesDock (hidden lg:block) vanished below breakpoints → no manual editing on tablet/phone. FIXED: both docks are slide-in drawers below lg (top-bar Layers/Sliders toggles + backdrop + DrawerClose header), static columns at lg+. Verified via Playwright at 390/820/1440 (drawers open on-screen; columns on desktop). Committed 68837aa. AUDIT (subagent, 9 findings) FIXED: (1) mode-switch dropped draft.id → StudioStage now locks tabs to the editing family+Preview when draft.id set; (2) occlusion opt-out→opt-IN (draftToPayload writes config.occlusion=true only for enabled 3D; experienceToDraft reads ===true; Booth gate ===true; removed vestigial event occlusion switch) so existing experiences unchanged; (3) headScale flush on unmount; (4) DnD pointercancel + unmount listener cleanup; (5) head-size tooltip corrected (sizes occluder not props); (6) SceneDirector accept synchronous ref guard (no double-charge); (8) ?id load alive-guard. NOTED not-fixed: (7) nose face-shell may clip noseBridge glasses lenses — needs live visual check, per-experience toggle mitigates. Committed 0457260. lint · 225 tests · build ✓ · harness absent from prod. Pushed (rebased onto CI assets commit e47fced).
- P5 (2026-07-09) — RESULT: DELETED superseded creators (Creator2D, Creator3D, creator3d/{ModelCanvas,LiveCanvas,AnchorPanel,PropertiesPanel}) after REFERENCE SWEEP (kept AiGeneratePanel/AnchorDots/ReferenceHead — still used by studio). Restyled AiGeneratePanel + Assets.tsx to platform tokens; AnchorDots gold→beam accent. platformGuide.ts: Studio + Scene Director section + how-tos (copilot answers studio Qs, no deploy). Grep gates: 0 legacy classes, 0 emoji glyphs in studio surface. Tooltips on mode switcher/3D controls/calibration/publish/feature/scene button. CHECKPOINT: lint · 224 tests · build ✓ · dev graph transforms clean post-deletion.
- P4 (2026-07-09) — RESULT: AI Scene Director. Edge fn ai-event-designer v10 DEPLOYED (Supabase MCP, verify_jwt on, additive mode:'scene': buildScenePrompt+buildSceneSchema, planJson STRING; existing create/copilot modes untouched) + identical code committed (lockstep). Client SceneDirectorPanel.tsx (Modal; invokes mode:'scene' with shaderCatalog from FILTER_SHADERS + headPieceIds from HEAD_PIECES; planFromJson normalize/clamp; per-piece accept: shader→createExperience free, frame→generateImage 1cr + best-effort config.scene tag, headpiece procedural→createExperience free or generate→generate3d 10cr; honest ai_key_invalid/rate_limited/not_configured banner). Wired into StudioShell: top-bar "Scene Director" button + ?scene= prefill. sceneDirector.ts stays pure (network call lives in the panel). CHECKPOINT: lint · 224 tests · build ✓. Committed 60f902f. NOTE: Gemini key rejected in prod → scene mode returns ai_key_invalid until a valid key is set (existing launch gate); panel degrades honestly.
- P3 (2026-07-09) — RESULT: hand-rolled pointer DnD. useStudioDnd hook (movement threshold distinguishes drag from click via consumedDrag(); window listeners read payload from ref not state to avoid stale closure; 2D drop→SET_KIND+SELECT_BUILTIN/SET_OVERLAY_UPLOAD+SET_TRANSFORM(pointToTransform2D); 3D drop→SELECT_HEAD_PIECE/SET_MODEL_ASSET + nearestAnchor snap via headMatrixRef). DragGhost portal. FaceRig +matrixRef writes group.matrixWorld.elements each visible frame → Studio3DView live → StudioShell headMatrixRef. AssetsDock drag sources (built-ins + head pieces) onPointerDown=beginDrag, onClick guarded. Stage drop-highlight ring. Higgsfield bust: image nano_banana_2 (903223cb) → image_to_3d (500f1153) → GLB at d3u0tzju9qaucj.cloudfront.net/...95db6f33...glb → remote-assets.json path public/models/reference-head.glb (CDN 403 in sandbox, CI fetches on push). Assets.tsx restyled to tokens + classifyAsset. CHECKPOINT: lint · 224 tests · build ✓. Committed 9e096c0.
- P2 (2026-07-09) — RESULT: unified StudioShell replaces the 2D/Shader + 3D-Anchor tabs. New src/components/studio/: StudioShell (reducer + ONE useCameraStream video#studio-video + studio settings + save via draftToPayload + ?id/?debug=occluder + StudioRedirect export), StudioStage (persistent video + shader RAF gated to 2D-shader + 2D overlay pointer-drag + in-canvas 2D·3D·Preview pill w/ Tooltip + 3D live/orbit/pause), Studio3DView (ports ModelCanvas orbit + LiveCanvas live, shared camera, occluder), StudioPreview (StageCanvas+Overlay3D booth-parity), AssetsDock (kind selector + shader/border/sticker/headpiece lists + upload + AiFramePanel + AiGeneratePanel), PropertiesDock (kind-adaptive sliders + headScale calibration + occlusion toggle + booth icon), StudioControls (StudioSlider/StudioToggle/SectionLabel), AiFramePanel (ports MagicGenerate). ar/ReferenceBust (GLB loader fetches public/models/reference-head.glb, procedural fallback). EventStudio: 12→11 tabs (Studio replaces the two creators); creator/creator3d→StudioRedirect. App.tsx adminRoutes: admin/studio + redirects. Library deep-links → /studio. CHECKPOINT: lint clean · 224 tests · build ✓ (OBJ inlined in bundle, verified) · dev transforms all studio+shared modules 200. Committed 72a8df1. NOTE: full camera+auth E2E blocked in sandbox (no camera, *.supabase.co blocked) — user must drive live.
- P1 (2026-07-09) — RESULT: FaceOccluder.tsx (canonical face BufferGeometry via `?raw` import + parseObj, cached module-level; cranium ellipsoid CRANIUM; meshBasicMaterial colorWrite:false renderOrder:-2 raycast no-op; debug wireframe mode). FaceRig +occlude/headScale/debugOcclusion props → renders FaceOccluder as sibling of AssetGizmo inside tracked head group. db.ts getStudioSettings/setStudioSettings (app_settings key 'studio', normalizeStudioSettings). Overlay3D +occlude/headScale → FaceRig. Booth.tsx fetches studioCfg for source==='db' only (legacy/code events unchanged), occlude gated `source==='db' && studioCfg.occlusion && attachExp.config.occlusion!==false`. LiveCanvas occlude={hasAsset} (interim verify). REFERENCE SWEEP: 4 FaceRig/Overlay3D sites, DemoBooth intentionally unoccluded. CHECKPOINT: lint clean · 224 tests · build ✓ (raw OBJ bundled).

## Open items
- PUSH e7e97dc + refresh PR #14 description once the user asks for a push.
- UI/UX MED findings not fixed: (4) Mine tab shows a slow spinner while experiences load; (5) mobile top-bar buttons are icon-only (no labels). LOW deferred: occluder only renders on the FIRST occlusion-enabled 3D piece — later pieces under-occlude (Studio3DView occluderIdx).
- Live-hardware verification (user): real-face tracking in studio 3D Live + booth occlusion — headless harness cannot render WebGL/camera; sandbox also blocks the landmarker MODEL fetch (storage.googleapis.com), so only WASM-init is provable here.
- Optional P5 stretch: copilot suggest_scene action — skipped (scope).
- Prior-session platform gates unchanged: valid GEMINI_API_KEY (user), Stripe LIVE keys, rate limit notes in docs/AGENT-ROADMAP.md Phase 3.

## Failed attempts
(none)
