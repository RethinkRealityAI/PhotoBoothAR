## Goal
AR POWER-UPS (2026-08-04, branch `claude/ar-optic-blast-hand-tracking-luznr3`, PR #39 draft): optic blast + MediaPipe HAND tracking + the interactive add-on system. SHIPPED IN THIS BRANCH: hand-gesture trigger sources (fistClench/palmOpen/pinch/peaceSign/handToTemple — reference math from the PlayCanvas ar-optic-blast example) joining the existing engine's score map; `beam`/`animate` trigger actions (jsonb-gated, legacy fixture-tested); BeamFX renderer INSIDE Overlay3D's canvas (captures get it free; charge→erupt→hold→fade envelope in pure beam.ts); handTracking/handRig (CPU delegate — GPU kills the shared GL context; 66ms gate interleaved with the face gate, 150ms idle back-off because "no hand" runs the palm detector every call; handedness label SWAPPED — MediaPipe assumes mirrored input, we feed raw); handPose.ts (weak-perspective depth over palm landmarks {0,5,9,13,17}, Kabsch-style basis from METRIC worldLandmarks via 100·diag(1,−1,−1)); HandRig + depth-only HandOccluder (21 spheres + palm slab, occluders SHRINK 0.9× never grow); `handAnchor` threaded layer→object→piece through the ONE mapper (lone hand object forces config.layers — the anyCustom trap); Power-Ups shelf (addOns.ts registry; Text3DBuilder retrofitted; PowerFxBuilder = live fxBus/BeamFX preview, one Add = object+customization+trigger, beam triggers carry NO objectId by design — emitter defaults to first 3D piece); guest lens-colour swatch row (guestPalette.ts; guestPick region flag; recolours lens AND 'auto' beam in one pick); 3 Meshy library assets (cyclops-visor/wizard-wand/power-gauntlet) with descriptors AUTO-prepared by src/dev/assetAutoPrep.ts (region classification from albedo texels under each vertex's UV; refLuminance MEASURED — headless Playwright drives it via the '--no-proxy-server' Chromium at /opt/pw-browsers/chromium); visor/wand/gauntlet PieceKinds (visor BEFORE glasses — old route demanded an EMPTY lens) mirrored into ai-generate-3d; 3 starter scenes with shipped triggers; Director plans may carry triggers (validated by REUSING parseTriggers; added on first approval).
DEPLOYS NEEDED AT MERGE: `ai-event-designer` (scene prompt teaches triggers) + `ai-generate-3d` (visor/wand/gauntlet kinds) — both esbuild-bundled + syntax-checked this session.
NOTED (not done): copilotExperience.ts Power-FX builder for the copilot; PickerDrawer surfacing of the guest swatch row; Studio3DView renders hand-anchored pieces inside FaceRig (visually head-glued in the 3D editor view only — booth/preview correct); wand grip alignment may need per-asset rotation tuning on a real phone; on-device dual-landmarker FPS check is owner-side (kill switch: HAND_DETECT_INTERVAL_MS 66→100).

MERGE STATE (2026-08-02): PR **#35 MERGED to main** (3b35b7b) — Higgsfield provider integration (BYO org keys, real API client, /host/import), Beamwall Frame Forge (live + published on the Higgsfield marketplace), the AR-features booth landing film, and the 12-finding adversarial-audit hardening. This branch (`claude/customer-journey-platform-polish-ejozbj`, PR #36 — customer-journey polish · guest UX · landing CMS · admin health; its migration 030_landing_content + admin-api v16 are ALREADY LIVE) was reconciled with that main and IS ALSO MERGED (34d6e13). Since then: PR #38 week-one social marketing kit merged (6ef1464). FRAME FORGE V2 (2026-08-02, app repo only — no platform files): browser-first mobile rebuild re-measured clean live (2-col 167px tiles, Vault config sheet, 3 tabs, 0 sub-44px targets, all 22 curated presets have real composited example art), audited adversarially (10 findings) with the confirmed fixes (phone error surfacing, retry-uses-submitted-settings snapshot, breakpoint state loss, refresh-vs-paginate, schematic caption) applied and redeployed. CONFLICT RESOLUTIONS in this merge: booth-feature.mp4/poster = the AR film (#36's 7/31 booth recut was superseded by the owner's 8/01 AR-film directive; its wall/challenges/cards recuts ship); Billing.tsx = #36's toast plumbing + #35's Connected-accounts card; remote-assets.json = union (AR clips + hero pngs); beamwall-video.md = union of both learnings appends. NOTE: repo now has TWO migrations numbered 030 (org_provider_keys + landing_content) — both live, do NOT renumber (010_orders precedent); next is 031.
PRIOR: PR #33 studio polish + the baseball-cap first library asset merged at ead18ab; PR #30 (platform redesign) = 85359fa; PR #31 (asset configurator) = f126ef9.

2026-08-04 FIX ROUND (owner phone-test findings, same branch/PR #39): PER-ASSET BEAM EMITTERS — `AssetTemplate.emitter {position,direction}` (GLB-local, drop-on-invalid like textSlots) + `fxBus` emitter REGISTRY (`registerFxEmitter` stack-per-key) + `FxEmitterPoint` (an Object3D sibling of the piece's Model INSIDE its transform chain, so rig pose/anchor/scale/mirror/animation apply free) + BeamFX bolts now WORLD-SPACE following the registered emitter's matrixWorld per frame (freeze-and-fade when the chain goes invisible or unmounts; per-origin fallback when never registered); `makeBeamSpec` 'auto' origin resolves by the EMITTING PIECE (hand-anchored → hand family), explicit origin drops the emitter key; `ScenePiece3D.fxKey` = layer/object id via the ONE mapper; Booth/StudioStage emitter scans are VISIBILITY-filtered (named-but-hidden → null, never another asset's colour/muzzle). WAND EMITTER WATCHOUT: whole-mesh PCA said shaft=+z but that axis is flat ribbon geometry — the VISIBLE shaft + both crystals run along X (grip's π/2 z-rotation maps +x = up out of the fist); emitter re-measured to the +x gem cluster [0.968,-0.004,0.006] dir [1,0,0]. Studio hand parity: Studio3DView live splits head/hand exactly like Overlay3D (HandRig at HAND depth — the "Preview looks 2× bigger" CAUSE was FaceRig-at-head-depth; occluderIdx over the head subset), orbit gets `ReferenceHand` (mannequin + HAND_REF_ANCHORS as gizmo bases), AnchorDots hide for hand-tracked selections; HandRig/HandOccluder now SELF-INIT + SELF-DETECT (FaceRig idiom — a wand with no hand trigger tracks anyway); needsHands in Booth (from RAW layers — reveal-hidden wand still preloads the 7.8MB model) + StudioStage; stageStatus gains faceNeeded/handNeeded/handVisible ("No hand yet"; hand-only scenes never coach a face; Booth hint swaps to "Show your hand to the camera" via onHandVisible). Authoring UX: PropertiesDock "Tracks on Head|Hand" switch (SET_OBJECT_TRACKING: family switch zeroes offset/rotation, KEEPS scale; grip/wristBack/palm chips in hand mode — wand-as-earring works both ways); same-SLOT add-conflict confirm (slotConflict: same anchor or same handAnchor, never whole-family — crown+glasses stays silent) in AssetsDock (ConfirmModal Replace/Add-both; Replace = DELETE → add → RETARGET_TRIGGERS repointing beam/animate objectIds at the replacement) + inline in PowerFxBuilder; PowerFxBuilder also gained draft-prop cap guards (ADD past MAX_TRIGGERS/MAX_OBJECTS is a SILENT reducer no-op — the modal used to report success on gear that never landed), a "Test blast" chip, gear-emitter preview registration ('@powerfx-gear'), and beamRegionId instead of a literal 'lens'. Gate: tsc 0 · 2151 tests (110 files) · build ✓ · legacy ✓ · screenshot-proofed (wand-tip + gauntlet-palm(−y) + visor-lens beams, tracking switch, both conflict UIs, orbit hand, "No hand yet" pill). DECISION: saved scenes carry FROZEN template copies, so emitters do NOT retrofit old scenes — re-add the gear (owner's `cap-test-event` class scenes included). NOTED (not done): per-object emitter nudge/beam-width refine deliberately unbuilt — template-authored emitters are the scalable path; fxBus broadcasts globally so builder test-blasts also fire a preview mounted behind the modal (harmless); `handToTemple` needs the FACE tracker too (a hands-only-working session never fires it).

2026-08-04 ROUND 3 (owner's second phone-test list, same branch/PR #39). An 8-agent investigation established every cause with measured numbers before any edit. LANDED so far: **mirrorGeometry.ts** (one asset serves both hands — negate x on positions/normals, flip tangent handedness, reverse winding; vertex ORDER preserved so packed region ids stay aligned; `scale.x=-1` was rejected because it renders the asset inside-out) · two Meshy hand mannequins vendored (`reference-hand-open/fist.glb`, untextured like reference-head.glb, 330/409KB) · **THE SIZING BUG FIXED**: the orbit bust is fitted by an anchor-CLEARANCE criterion, which is an absolute-cm target and so never constrained SIZE — measured against the canonical face along the 12 anchor directions it landed at **0.7696x**, i.e. props read **1.30x oversized in orbit only** (live/preview were always right; the fov 42-vs-63 difference is only 2.6-6.7% parallax, an order of magnitude too small to be the cause). `normalizeFitToCanonical` (pure, +4 tests incl. a real-canonical-geometry regression) grows the fit by the MEDIAN radius ratio about the head-space origin; orbit now also applies headScale to the reference head, its anchor dots and its anchor bases exactly as FaceRig does · **PRESETS** owner-tuned on a LIVE camera (C1 resolved from the screenshots: visor 8.60 + necklace 1.89 were tuned on the camera feed; the gauntlet's 15.80 IS the computed default, so no double-correction): visor fitCm 15→16.33 + brow nudge + occlude, gauntlet gains defaultRotationDeg (-83,5,174) that lands it ON the wrist, new `defaultRotationDeg`/`defaultOcclude` on ConfigurableAsset, SET_MODEL_ASSET takes DEGREES and stores radians, both add paths pass them, and the jewelry builder now carries placement on the ADD instead of a follow-up SELECT_ANCHOR (which deliberately zeroes offset+rotation and was silently wiping it).
ROUND 3 IS COMPLETE — every measured cause is fixed and gated (tsc 0 · **2229 tests / 113 files** · build ✓ · legacy build ✓), commits `ba4aa5e` → `4a6c855` → `11566a3` → `e34459c` → `978cc72`:
- **Phantom hand fires** — the loop steps on the FACE clock (33ms) while hands update at 66/150ms, so one 1.0 sample was REPLAYED into the EMA and read 0.75 past enter 0.62 (reproduced at t=99ms). `engine.step()` now takes a stale-source set: a source with no new sample HOLDS (a held gesture must not decay between inferences) and a stash older than 250ms feeds ZEROS.
- **Pinch was mathematically unreachable, not mistuned** — it multiplied by wrist-relative index EXTENSION, which pinching necessarily shortens (firm pinch 0.297 vs enter 0.60). Rebuilt on d(4,8)/d(5,0) with an uncurled guard → 1.000.
- **Aspect distortion** — every 2D gesture distance ignored aspect and stretched x by 1.78× at 9:16 (the same pinch scored 0.078 there); distances are now image-height units, invariance-tested at 4 ratios.
- **Occlusion honesty** — the toggle DID dispatch; exactly ONE FaceOccluder renders per canvas (gated on the FIRST head piece opting in), so a per-object switch on any other piece did nothing, and occlusion is opt-IN, not on by default. Now ONE scene-level switch (`SET_SCENE_OCCLUSION`, OR-read / write-all, the per-object field preserved), default ON for NEW drafts only — hard-defaulting saved scenes would start depth-clipping live events.
- **Thumbnails had NEVER paired** — `uploadAsset` appends an extension to a name that already has one (`crown.glb` → `<uid>-crown.glb.glb`) while the thumb normalized to `crown`, and the fixture asserted a name `uploadAsset` cannot produce; `extFor` also stored a `model/gltf+json` file as `.gltf.png`. Auto-capture now runs on add for uploads and Power-Up models.
- **3D organization + gizmo** — head/hand FrameScenes with a focus switch, orbit rotate-about-anchor, and a live-mode gizmo (`holdPose`) so hand gear is draggable in camera mode; the toggle is labelled. Real Meshy hand mannequin at true metric scale (9.995/6.132cm vs canonical 9.851/6.220) with mount points DERIVED from `HAND_ANCHORS` in a pure module.
- **Unified upload** — ONE drop zone at the very top of My Assets takes art or models (reuses `classifyAsset`, dragenter/dragleave COUNTER); **tracking chip** names both families and says "`<gesture>` detected".
- Also: 4 divergent head/hand family predicates unified (two disagreed, so an unknown anchor id downloaded the 7.8MB hand landmarker for a HEAD piece).
- **BOTH HANDS FROM ONE ASSET** (`5dec920`) — `mirrorGeometryX` is now WIRED. `AssetTemplate.modelledHand` declares the hand a GLB was authored for (gauntlet 'left', per the owner's phone test); absent = hand-agnostic and nothing ever flips, which is every pre-existing descriptor. `HandRig` publishes the tracked hand through a context (`components/ar/handMirror.ts`) that `Model` and `FxEmitterPoint` subscribe to, so a surface mounting them inside a HandRig gets the behaviour without threading a prop — the same anti-drift shape as the one piece mapper. Per-object `handFit` ('auto'|'left'|'right', absent = auto) pins a piece so it NEVER consults the tracker: handRig's handedness label swap is reasoned from MediaPipe's source and has never been checked on a phone, so a pin is the owner's way out if a device reports it the other way round. `handFit` needs no `config.layers` force-predicate entry because it can only exist beside `handAnchor`, which is already in it. Geometry is swapped in a dedicated effect (not a re-clone), so switching hands costs a WeakMap lookup rather than a 12MB re-parse; `mirrorGeometryX` now gives its result its OWN `userData` because three's `BufferGeometry.copy` assigns it BY REFERENCE and FaceRig stamps the region-id key there. Mirroring is SKIPPED (with a warning) for a template carrying text slots — a decal is carved against the surface it was built for; no shipped hand asset has one. VERIFIED on the real 33,005-vertex / 30,697-tri gauntlet in headless Chromium: bbox mirrors exactly (x [−0.5410,+0.5392] → [−0.5392,+0.5410], y identical), signed volume UNCHANGED at +0.37137 (winding preserved — a flip would have negated it), outwardness identical at 0.7536, 0 region-id mismatches, 0 position mismatches, and the two renders differ in thumb side while both light as solid surfaces.
WATCHOUT proven by screenshot: the stage's mode band is `inset-x-2.5 z-30` (full width, purely to centre its pill) and swallowed every click on the z-20 focus switch beneath — any floating control on that strip needs `pointer-events-none` on the band with `pointer-events-auto` on its own controls.

2026-08-05 ROUND 4 — HANDEDNESS + FOREARM (same branch/PR #39). Owner: "we also have both left and right, so let's utilize them accordingly"; "add a right-hand masking model… if I have a left-handed object but only a right-handed masking model: it's a weird experience trying to fit it"; "make sure the hand tracking works well, and even with arms, that content will kind of stick to the user's arm"; "free reign to change and adjust everything".
**THE UPDATED GAUNTLET WAS NEVER FOUND** — PhotoBoothAR's newest push is still my own `6e2c165`, no new branch exists, and no other repo on the account carries a gauntlet (`asn`, the only repo pushed more recently, is an unrelated storefront). Everything below therefore keeps the single-GLB + mirror path working, and `AssetTemplate.mirroredGlbUrl` lets a real handed pair drop in later with NO render-code change (present ⇒ load it, absent ⇒ mirror).
**THE MANNEQUIN WAS THE BUG THE OWNER FELT**: both vendored mannequins are RIGHT hands and `ReferenceHand` took only `pose`, so a left-modelled gauntlet was fitted against its own mirror image and every offset tuned against it was tuned backwards. `previewHand(modelled, fit)` now hands the mannequin; a human left hand IS the mirrored right hand, so this is exact. MEASURE-THEN-MIRROR IS LOAD-BEARING: `measureHandMannequin` takes `right = up × normal`, and a cross product NEGATES rather than mirrors under a reflection, so measuring a mirrored mesh inverts the finger ordering — independently reproduced at **+8.7% scale drift on the fist mannequin** (ring MCP read as middle). Measure the known-good right hand, bake the fit into the geometry, then `mirrorGeometryX`.
**HALF A MIRROR IS WORSE THAN NONE** (caught by adversarially reviewing my own commits): `mirrorGeometryX` flips vertices about the MODEL's local plane while the authored offset/rotation stay put, so the gauntlet mirrored to a pose BESIDE the hand. `mirrorPlacement` reflects the transform too (negate offset.x; for XYZ Euler keep rx, negate ry/rz) and is an INVOLUTION, which is what lets the orbit gizmo edit a mirrored piece and write back in the asset's own frame. Also: the orbit mirrored the mannequin but never the asset (the decision is published by `HandRig`, and orbit has none) — pinned cases were actively contradictory; and the mannequin mirror LEAKED two geometries per Fits click (`mirrorGeometryX` caches on its source, which was a fresh clone every run, so the cache could never hit).
**FOREARM — DELIBERATELY NOT A THIRD MODEL.** HandLandmarker exposes nothing above the wrist (verified in the shipped `vision.d.ts`: the result is exactly landmarks/worldLandmarks/handedness/handednesses). The only honest inference is that the arm continues along the hand frame's −Y, exact at a neutral wrist and wrong by the wrist's deviation otherwise — MEASURED 2.6° relaxed, 28.4° on a gripping fist against a PoseLandmarker-derived elbow. Drift = reach·sin(θ), and a forearm is only ~5–6cm in radius, so **FOREARM_REACH_MAX_CM = 12** is the hard cap; new `forearm` anchor + `alongForearmCm` + 4 occluder beads use it. PoseLandmarker was measured and REJECTED: +5.78MB, +571ms init, ~0.92× the hand landmarker's per-call cost as a THIRD blocking inference on a main thread near 58% duty, and on this app's own portrait framing the elbow left the frame in 2 of 16 samples (median visibility 0.67). HolisticLandmarker is a hard no — it has no `facialTransformationMatrixes`, which is the entire head-pose engine. Industry check: 8th Wall's hand model "extends just past the palm to include the region where a watch or bracelet would typically rest" + a `wrist-occluder`; Snap ships Wrist Binding; WebAR.rocks masks the inside of a bracelet. **None of them tracks a forearm — the occluder, not the axis, is what sells the shot.**
GAUNTLET PLACEMENT DERIVED, PARTLY: measured over all 33,005 verts, the long PCA axis runs to the fingers [0.249,−0.132,0.960] and the palm-outward normal is [0.030,−0.989,−0.143] — the latter independently agreeing with the authored palm emitter firing along GLB −y. Rotating those onto the hand frame gives `defaultRotationDeg (−98.49,−14.01,−3.78)`, replacing hand-tuned values that crossed the gauntlet's fingers over the mannequin's by ~30°. The SAME derivation applied to the OFFSET put the piece ~5cm too high, so the owner's nudge stays — a derivation that fails its own screenshot does not overrule a human who checked it on a hand. Size unchanged: the hand portion is 1.209 of 1.898 longest units, so fitCm 30 renders 19.1cm of hand against an adult mean of 18.6cm. The gauntlet is genuinely forearm-length — its cuff runs 0.725 units ≈ 11cm past the wrist (wrist = the narrowest station, 189 verts at t=−0.461 against 1041 in the cuff and 2606 in the fingers).
Also: camera-error scrim no longer covers 3D-Orbit (it needs no feed, but `showVideo` was true there, so a webcam-less host got a full-bleed NO CAMERA FOUND over the one view that works without one); hand mounts gained worked-example tooltips like head anchors always had (Wrist vs Palm differ by 0.3cm and one landmark pair); hand-tracked library tiles now badge "Hand"; "Fits" shows for EVERY hand piece (a symmetric wand still has to be placed in one hand). Gate: tsc 0 · **2271 tests (114 files)** · both builds · orbit screenshots at Fits Either/Left/Right.
ROUND-4 FOLLOW-UP (from the workflow's own synthesis, which measured what I had just shipped): the mannequins are HANDS — only **4.07cm** of mesh below the wrist (open) and **1.02cm** (fist) — while the new `forearm` anchor mounts at −5cm, so a bracer hung 0.9cm past the end of the open mannequin and 4.0cm past the fist, in empty space. `ReferenceHand` now draws a tapered `ForearmStub` of exactly FOREARM_REACH_MAX_CM at the occluder's own radii, so arm gear has an arm to fit against AND the host can see where the trustworthy reach ends. Also closed a real hole in my own mirror: the engraving guard suppressed the MESH mirror but not the PLACEMENT mirror, which is the "worse than not mirroring" state — `canMirrorAsset` is now the one gate both halves ask (a purpose-built pair GLB is deliberately NOT blocked: it really is the other hand, so its placement must still reflect).
NOTED (not done, round 4): the occluder audit's D2 (per-landmark x/y unprojected at the PALM's depth, not the landmark's, so a sphere does not lie on the ray through its own pixel) is real and unfixed; `HandOccluder`'s header still claims ~500 tris against an actual 3,080; the orbit's `handRefAnchorPoint` forearm mount is untested against a tracked arm.

## Now
2026-08-07 PUBLIC GUIDES + DOCS AGENT (branch `claude/automated-docs-agent-o6mc4i`, **PR #41 — MERGED to main 2026-08-08**): public `/guides` surface COMPLETE — 5 guides (make-a-frame · make-3d-props · use-the-studio · first-event · run-the-night) at a lazy route excluded from the legacy bundle by the `import.meta.env.VITE_EVENT` dead-code fold (grep of dist-legacy = EMPTY, gated); 14-frame downloadable pack (Recraft V4.1 via Higgsfield → green-window convention → keyed by chromaKey.ts through `scripts/key-guide-frames.mjs`, ~112 credits total incl. regens) in `public/guides/frames/` + thumbs; 15 prompt cards all test-asserted to end in `GREEN_TAIL`; 2 guide films (beamwall-video pipeline, 1280×720 ≤1.4MB); interactive hotspot screenshot of `/dev/studio` (12 markers, coords authored from the committed PNG, IHDR-size-asserted). SUSTAINABILITY: `src/lib/guidesDrift.test.ts` = two-way coverage contract keyed on visibleStudioTabs/visibleHostNav/ADD_ONS/feature keys/trigger registries — proven RED on 4 deliberate breaks with actionable messages, restored green; `.claude/agents/beamwall-guides.md` owns procedures + Learnings; CLAUDE.md routing row + PROJECT.md finishing-task item 7 added; monthly refresh Routine created. Facts corrected en route: 9 trigger sources (not 7). WATCHOUTS captured in the agent Learnings: Higgsfield batch >4 trips a false "Out of credits" rate guard; EVERY face-window/character frame design paints a face into the green slot unless the prompt says "COMPLETELY BLANK — no face, no head"; EventStudio chrome is fetch-coupled → no deterministic harness, covered by a steps block instead of a second hotspot shot. Gate: tsc 0 · full suite green (2315: 2312 + 3 media-existence) · build ✓ · legacy ✓ + grep EMPTY · sweep CLEAN ×18 (390/820/1440 × 6 routes, hotspot sheet verified at 390).
PRIOR: Round 3 COMPLETE and pushed on `claude/ar-optic-blast-hand-tracking-luznr3` (PR #39 draft, head `978cc72`): sizing + presets, gesture correctness, hand mannequin + 3D organization/gizmo, occlusion honesty, thumbnails, unified upload zone, dual tracking chip. PR #39 stays a draft pending the owner's phone re-test; the two edge fns (`ai-event-designer`, `ai-generate-3d`) still deploy AT MERGE.
NOTED (not done, round 3): the fist mannequin's two span estimates disagree 32% (fit is their mean); a mirrored asset with TEXT SLOTS renders un-mirrored by design (the decal basis flips with the mesh, so the engraving would read backwards) — none ship today; the orbit mannequin's own handedness is undeclared, so the editor preview never mirrors (only the tracked views do); each uploaded model triggers one in-memory GLB capture on add (a viewport-lazy variant is the follow-up); orphaned old-scheme thumbnails remain in storage (no delete without approval); dead `Sparkles` import at StudioStage.tsx:19; pre-existing truthiness checks `if (i.toast)` / `if (i.camError)` in stageStatus.ts.
BOTH platform PRs are on main; production deploys from 34d6e13. Netlify prod build pending → owner phone-verifies film autoplay + play/pause chip against beamwall.netlify.app. Live infra now: migrations through the two 030s (org_provider_keys + landing_content — do NOT renumber, next is 031) · admin-api **v16** (landing CMS actions) · ai-generate-image **v17** (Higgsfield provider) · provider-keys v2 · import-asset v2 · other fns as at the 2026-07-28 ship. /admin/landing is live end-to-end (editor → publish → anon read via get_landing_content()).
2026-08-04 (branch `claude/beam-wall-teaser-video-gbv0et`): KINETIC TEASER FILM. New composition `hyperframes/studio/teaser/` — 1080×1920, 33.2 s, the repo's FIRST 9:16 composition. It translates a reference ad's motion language (near-black canvas · one persistent wordmark · letter-assembly with directional colour streaks · perspective card deck · layer separation with design-tool selection handles · coverflow carousel · corridor flythrough · bloom end card) onto six real shipped features: 01 frame designs · Frame Studio · 02 personalisation · 03 AR booth · 04 challenges · 05 keepsake cards · 06 live wall. Master committed at `docs/marketing/beamwall-teaser.mp4` (4.9 MB, music bed baked) — it is a SOCIAL master, deliberately NOT wired into Landing.tsx and nothing imports it. Imagery: 6 new Higgsfield plates (12 credits) vendored through the `scripts/remote-assets.json` CI path into `hyperframes/studio/assets/photos/teaser/`, plus the already-committed hero captures, photo cells and AR footage. The workflow's compress step gained a rule for that dir (2k png → 1080w webp, pngs dropped) so the 6 plates cost 1.1 MB instead of ~45 MB. No app code, no schema, no edge fn, no deploy. Three defects found by verification and fixed before ship: the shipped `frame-*.webp` are finished EXAMPLE images (model baked in) that hid every guest photo when used as an overlay — frames are now drawn in CSS; a dark-photo cross-dissolve read as double exposure — the deck now peels fast and cuts on a flash; and `.gcel` lacked `box-sizing`, wrapping the live-wall payoff grid to 5 rows that ran off frame. Full detail in the beamwall-video learnings log.
PRIOR: BOTH PRs are on main; production deploys from 34d6e13. Netlify prod build pending → owner phone-verifies film autoplay + play/pause chip against beamwall.netlify.app. Live infra now: migrations through the two 030s (org_provider_keys + landing_content — do NOT renumber, next is 031) · admin-api **v16** (landing CMS actions) · ai-generate-image **v17** (Higgsfield provider) · provider-keys v2 · import-asset v2 · other fns as at the 2026-07-28 ship. /admin/landing is live end-to-end (editor → publish → anon read via get_landing_content()).
EDGE-FN WATCHOUT (PR #28 lesson, still binding): tsconfig excludes supabase/ and esbuild only PARSES — bundle with esbuild and check identifiers before any edge deploy.
GATE METHOD (still binding): gate the COMMIT in a detached worktree (symlink node_modules, no .env.local), never the working tree.

## Next
- **HIGGSFIELD OWNER STEPS (PR #35):** create a key pair at cloud.higgsfield.ai and set `HIGGSFIELD_API_KEY` ('keyId:keySecret') in Supabase secrets — until then the Higgsfield option degrades to ai_not_configured and only BYO-key orgs can use it. The REST contract is UNVERIFIED against live — watch the first real generation (submit-response id field + status-body nesting). Also unverified whether platform API keys draw from the consumer credit pool — check the cloud.higgsfield.ai billing page.
- HIGGSFIELD FOLLOW-UPS (named, non-blocking, from the #35 session): Frame Forge live generate→key→send round trip needs a signed-in human run; 13/22 Forge presets render layout schematics (generate example art in-app); Forge cover lacks the HF glyph; Forge tile chips 32px vs 44px touch rule; Copilot FAB overlaps /host/import fine print at 390px; platform fetches Google Fonts at runtime (venue-wifi risk, pre-existing); no rate limit on provider-keys/import-asset; ai-generate-image stores every provider result as .png contentType (import-asset sniffs correctly — port the pattern); redeploy ai-generate-image from repo at next ship to close a 6-comment byte diff.
- OWNER: verify film autoplay + the play/pause chip on your phone against PRODUCTION (your device reports Reduce Motion — the round-7 code gated autoplay on that preference, which is why the films never played for you; the gate is now removed for content films and a first-gesture retry was added for iOS Low Power Mode). NOT verifiable in the sandbox: its Chromium has no H.264 decoder, so only the `play()` trigger logic was proven (spied under emulated reducedMotion).
- OWNER: first /admin/landing publish (edit → Save draft → Publish); `hero-activation.webp` (v1, unreferenced) awaits delete approval.
- OWNER (unchanged): Stripe LIVE keys (#1 gate) · SUPPORT_NOTIFY_EMAIL · /admin/catalog Provision in Stripe · custom SMTP · vault service_role_key for ai-jobs cron · HaveIBeenPwned toggle.

## Constraints
- GUIDES ROUND 2 brief (2026-08-08, owner feedback on PR #41, verbatim): "Do NOT weaken any assertion"; "removing content a test iterates over is fine; deleting/loosening an assertion is not"; "Keep changes inside your owned files (guides components/content/tests)"; "Do NOT touch Landing, HostLayout, features.ts, or any product code."; "Do NOT git commit or push — the orchestrator owns git."; sweep dev server "npm run dev with inline VITE_SUPABASE_URL=... port 5173, HMR on; kill by PID via lsof -ti :5173 when done"; "the owner's bar is \"beautiful\", not just passing."
- GUIDES PHASE 1 brief (2026-08-07, branch `claude/automated-docs-agent-o6mc4i`, verbatim): "This is a MECHANICAL MOVE of shipped high-traffic code: move, do not rewrite, do not restyle, do not \"improve\"."; "Mind the comment around :893-896 about pill collision at 390px — do not change breakpoint classes."; "Do NOT touch src/lib/features.ts visibleHostNav."; "Do NOT git commit or push — the orchestrator owns git."; "Do NOT create src/pages/Guides.tsx or any guides content"; "No new npm dependencies. Change only what this task requires."; "start dev server (npm run dev, port 5173, HMR ON — NEVER set DISABLE_HMR)"; "Kill only PIDs you found via the port (lsof -ti :5173), never pkill by name."
- ROUND-3 WAVE 4 brief (2026-08-04, branch `claude/ar-optic-blast-hand-tracking-luznr3`, verbatim): "Do NOT run git commit or git push."; "vitest NODE env, glob src/**/*.test.ts ONLY (never .tsx), no jsdom; a test file must not import anything whose graph reaches src/lib/supabase.ts"; "Frozen legacy coded events must stay byte-identical — run the legacy build."; "No new npm dependencies, no migration, no RLS or edge-function change."; "Do NOT touch src/lib/handGestures.ts, src/lib/studio/triggers.ts's engine, src/components/ar/ReferenceHand.tsx or src/lib/studio/handRefAnchors.ts — they just landed."; occlusion: "DO NOT hard-default existing scenes on"; "Do NOT add a hand-occlusion control — HandOccluder is already unconditional"; "the per-object FIELD must survive; only the UI moves"; thumbnails: "Do NOT persist a per-layer thumbUrl on the experience jsonb"; "`glbThumb.ts` is browser-only and must never be imported from a `.test.ts`"; uploads: "Do NOT reuse src/components/upload/UploadDropzone.tsx as-is"; "Use a dragenter/dragleave COUNTER, not a boolean"; "do not write a second classifier" (reuse `classifyAsset`); chip: "Keep all the logic in stageStatus.ts … do not weaken existing assertions"; "Keep every string SHORT".
- STUDIO-3D brief (2026-08-04, branch `claude/ar-optic-blast-hand-tracking-luznr3`, verbatim): "Do NOT run git commit or git push — the orchestrator owns git."; "Tests are vitest NODE env, glob src/**/*.test.ts ONLY (never .tsx), no jsdom. Pure logic goes in .ts modules with colocated tests; components only wire. A test file must not import anything whose graph reaches src/lib/supabase.ts."; "Frozen legacy coded events must stay byte-identical"; "Do NOT touch the booth guest path (src/components/Booth.tsx), the trigger engine, or handGestures.ts — another change just landed there."; "If you add any new PER-OBJECT persisted field, it must join the config.layers force predicate in draftMapping.ts … Prefer view state over persisted state where possible"; owner: "we need better organization of the head and hand masks models in 3D mode, or perhaps another secondary tab to switch between them if both types are being used in the scene."; "make sure that in 3D mode, if we're using camera mode, the gizmo still works for the hand model."; "Perhaps add a little placeholder text for that icon and maybe change the icon so that it's a bit more clear for users what the toggle does"; "The assets are still a little big on the mask models... the mask models look small and don't match."
- User (2026-08-04, AR Power-Ups session, verbatim): "we also have meshy api and mcp  for 3d so lets use that first for any tmeplate assets we're creating."; "All triggers should be able to be used for any assets in general we can even combine them as we currently can"; "maintain some clean UI/UX to all of this without it becoming overwhelming"; hand anchoring: "Create the appropriate hand maksing models for all the different use cases".
- Standing (from prior sessions, still binding): never break legacy-events; no new npm deps; tests `.ts` only, never import anything reaching src/lib/supabase.ts; never loosen tenant RLS; liquid-glass idiom on platform surfaces; prefers-reduced-motion; Gemini key never committed; no VITE_EVENT in .env.local during tests; do-not-touch: StageCanvas.drawFrame semantics, booth capture/submitPost, experiences schema; FrameStudio.tsx + DemoBooth.tsx not deleted without owner approval; generic-only template library (no legacy-event branding).
- User (2026-07-30, verbatim gist): "polish and refine anything you think we need to in regards to the customer journey from login to deploying their first experience"; "refine the guest experience ... easy, smooth and amazing"; "admin portal ... especially want to be able to control the landing page via a cms so I can swap out images and videos as needed"; "utilize all necessary subagents"; "feel free to add any wow and amazing features"; "if there's anything for me to do, just note it but you should finish all you can without me".

- User (2026-07-30, landing round 7, verbatim gist): remove the red-marked highlights strips "in all the sections as it shows in the videos already"; "Just a nice one sentence hook then video" — text must not be "repetitive of the videos"; "the video should play automatically as they scroll to that area and it should show the playback controls... there should be play and pause, thats it. even then make it subtle and bottom right of the video"; "check all the videos again ensure they make sense have the best visuals and not redundant and are engaging and hook the user"; header frames get "photorealistic images of various types... by default we should use the ai ones, making sure they match the type for event the frames are for"; "make sure these images are also optimized webp so they load fast. same with any videos"; switchable via the CMS; free rein on other landing improvements — "slick, beautiful and not overly info dense".
- User (2026-07-28, personalization brief, verbatim gist): "for the frames ask users if they want any names or initials on them either if they provide a name for the event or who they're celebrating or a logo then give them options on how it can be implemented like monogram in cursive, bottom or top, within the frame or extending out or do they just want that with no frame. use higgsfield to create some sample images and types of frames covering all these use cases that users can select from in a nice a2ui widget… or from a pill suggestion"; "if you think this flow and thinking is useful for any other parts of the system feel free to implement it too"; "you too think of one thing that would blow the minds of users… and implement it. no need to ask for permission… all permissions are granted"; "verify all rigorously and ui changes with screenshots. like produce assets using these new pathways to ensure all is working and that all tests pass. making sure you always audit your work, checking for any logic gaps and challenge assumptions".

- Wave 7 brief (2026-07-29, admin/operator surfaces, verbatim): "Do NOT commit, push, or open a PR — the orchestrator owns git."; "No new npm dependencies. No migrations. No Supabase deploys. No RLS changes"; "admin-api asserts `is_platform_admin` BEFORE its action switch; do not weaken any authorization path."; "Pure logic in `.ts` + colocated `.test.ts`"; "A test file must NOT import anything whose module graph reaches `src/lib/supabase.ts`"; "Respect `prefers-reduced-motion`."; do-not-edit list incl. `src/lib/db.ts`, `src/lib/auth.ts`, `src/lib/host.ts`, `src/components/booth/**`, `src/components/studio/**`, `src/components/wall/**`.

- STAGE A brief (2026-07-29, asset-configurator engine, branch `claude/asset-configurator-pipeline`, verbatim): "Do NOT commit, push, or open a PR — the orchestrator owns git."; another agent owns STAGE B in `src/types.ts`, `src/lib/studio/state.ts`, `src/lib/studio/draftMapping.ts`, `src/components/Booth.tsx`, `src/components/studio/StudioPreview.tsx`, `src/components/studio/Studio3DView.tsx`, `src/components/studio/PropertiesDock.tsx` — "DO NOT EDIT THOSE."; "No new npm dependencies. No runtime CDN fetches"; "Tests: vitest NODE env, glob `src/**/*.test.ts` ONLY (never `.tsx`), no jsdom/RTL. A test file must NOT import anything whose module graph reaches `src/lib/supabase.ts`."; "Legacy coded events (hope-gala/jenna-jake/detola-wuyi) must be byte-identical when the new fields are absent."; "Respect `prefers-reduced-motion`."; "`src/lib/studio/finish.ts` and `src/components/ar/FaceRig.tsx` — extend, do not rewrite"; "`parts`/`label` ABSENT must be byte-identical to today's behaviour — that is the legacy guarantee."; build privately with `npx vite build --outDir dist-stageA --emptyOutDir`.

- AUDIT-FIX brief (2026-08-01, branch `claude/hicksfield-frame-assets-ud07ol`, verbatim): "Do NOT run git commit/push — the orchestrator owns git."; "do not touch supabase/functions/ai-generate-image or import-asset" (their halves of F1/F3/F7 + the `config.greenScreen` stamp are already fixed by the orchestrator); "Read each target file (or its enclosing regions) before editing, follow existing idioms, keep changes minimal."; "Do NOT rebuild the proposal surface." (F6); "Do NOT touch the meshy path." (F3 sweep).
- STAGE C brief (2026-07-29, configurator UI/library/prep tool, verbatim): "Do NOT commit, push, or open a PR — the orchestrator owns git."; "No new npm dependencies. No runtime CDN fetches. No migration, no schema change, no RLS change."; "Tests: vitest NODE env, glob `src/**/*.test.ts` ONLY (never `.tsx`), no jsdom/RTL. A test file must NOT import anything whose graph reaches `src/lib/supabase.ts`."; "Legacy coded events must stay byte-identical when the new fields are absent."; "Respect `prefers-reduced-motion`."; "keep the maths in the PURE `regionTint.ts` module"; "Respect the existing `sel3D.type === 'model'` gate"; "Reuse the existing control idiom (numeric entry + sliders + chips), do not invent a new visual language. Semantic tokens only."; "Set `preparedBy` honestly: `'auto'` only when no human touched it."; "Library content must be generic — no event-specific branding."
- HAND-TRIGGER FIX brief (2026-08-04, branch `claude/ar-optic-blast-hand-tracking-luznr3`, verbatim): "Do NOT run git commit or git push — the orchestrator owns git."; "Tests are vitest NODE env, glob src/**/*.test.ts ONLY (never .tsx), no jsdom/RTL. A test file must not import anything whose module graph reaches src/lib/supabase.ts."; "Keep pure maths in the pure modules (handGestures.ts, triggers.ts); components only wire."; "Legacy coded events must stay byte-identical: no stored config can name a hand source, so verify your change cannot alter behaviour when no hand trigger exists."; "Never weaken an existing assertion to get green; if an existing test contradicts the fix, quote old vs new and justify."; owner report: "the beams and stuff with the hand triggers sometimes fire without any hand in the frame … ensure that all the gestures are working, there are no logic gaps, and all assumptions have been challenged"; "I don't think the pinch is working."
- Stage B brief (2026-07-29, configurator persistence, verbatim): "Do NOT commit, push, or open a PR — the orchestrator owns git."; "DO NOT EDIT THOSE" (assetTemplate.ts / regionTint.ts / assetDecal.ts / finish.ts / FaceRig.tsx — the render stage owns them); "Do NOT change the experiences schema, do NOT add a migration, do NOT touch RLS."; "No new npm dependencies."; "Keep `src/lib/studio/controlSpecs.ts` as the single source of bounds … do not scatter new limits"; "[the `sel3D.type === 'model'`] gate is correct — respect it"; "Legacy coded events (hope-gala/jenna-jake/detola-wuyi) must be byte-identical when the new key is absent"; "Change only what the task requires".

## Decisions
- DECISION: landing CMS = singleton `landing_content` (draft/published jsonb) + SECURITY DEFINER `get_landing_content()` (anon reads PUBLISHED only; revoke-then-grant per the 022 lesson); writes via admin-api (audited) — platform_config was int-only/no-client-policies and an RLS admin-write table has no precedent.
- DECISION: landing media rides the existing public `assets` bucket under `_platform/landing/` — migration 018 already grants platform admins bucket-wide write and `_platform` can never be an event slug; zero new storage policy.
- DECISION: `DEFAULT_LANDING_CONTENT` media fields are undefined = "use bundled import" (Landing.tsx resolves), keeping landingContent.ts pure/test-importable and first paint instant; a total Supabase outage is invisible on the marketing page.
- DECISION: guest QR retarget to /welcome is gated on `RuntimeEvent.source === 'db'` — legacy walls keep byte-identical targets (legacy DOES have /welcome, but the sites are frozen).
- DECISION: Dashboard go-live no longer reloads the page; optional `onStatusChange` prop keeps EventStudio's pill in sync; legacy mounts pass nothing.

## Facts
- Commands: `npm run lint` (tsc) · `npm test` (vitest node, src/**/*.test.ts only) · `npm run build` · legacy `VITE_EVENT=hope-gala npm run build`. Install with `npm install --ignore-scripts`.
- Baseline at session start: tsc 0 · 1966 tests (99 files). After waves: 1990 tests (101 files) per both implementing agents' runs.
- New modules: src/lib/landingContent.ts (pure, +16 tests) · landingContentClient.ts · landingMedia.ts · src/lib/camera.ts classifyCameraError/CameraUnavailableError (+8 tests in camera.test.ts) · src/pages/admin/Landing.tsx (/admin/landing, NAV 'Landing').
- admin-api new actions: get_landing_content_admin / save_landing_draft (≤200k JSON guard) / publish_landing_content / revert_landing_draft — all audited; pre-switch guards untouched.
- store.ts: new `experiencesFailed` flag; PickerDrawer has loading / failed-retry / empty states.
- Playwright: executablePath '/opt/pw-browsers/chromium-1194/chrome-linux/chrome'. Sandbox egress: *.supabase.co and Netlify previews 403 — Supabase only via MCP.
- PR #36 (draft) targets main; Netlify deploy previews build per push.

## Done
- 2026-07-30 THREE AUDITS (host journey 26 findings · guest UX 28 findings · admin+CMS design) — RESULT: ranked implementation briefs, all file:line-cited.
- 2026-07-30 ADMIN HEALTH (commit 60c9f52): Payments renders LoadError instead of confident-empty on failed orders fetch; Credits failure states got Retry.
- 2026-07-30 CMS WAVE — RESULT: migration 030 (committed 2101e0e, NOT yet applied) · admin-api +4 audited actions (NOT yet deployed) · pure normalize layer (hostile-input tests: javascript:/data:/foreign-origin URLs rejected, fixed-slot arrays never resize) · /admin/landing editor (ConfirmModal publish, media slots Replace/Reset, LoadError/useToast, OG-static notice) · Landing.tsx reads content.* with bundled fallbacks; agent-verified tsc 0 · 1990/101 · both builds ✓.
- 2026-07-30 HOST WAVE — RESULT: 12/12 items (ToastProvider on /host + success toasts · Go live + Print signage on create-success · signup resend · live linked getting-started checklist w/ progress ring · Dashboard QR → /welcome + share link, platform-gated · go-live without reload · checkout-return toasts on EventsList/EventStudio · honest wizard steps + disabled-CTA copy · signup password reveal + promo echo · rail New-event CTA + per-path titles + sign-out confirm · platformGuide no longer advertises unmounted FrameStudio · template-seed failure notice); gated in isolated worktree tsc 0 · 1966/99 · both builds ✓.
- 2026-07-30 GUEST WAVE — RESULT: 15/15 items (12-CTA --on-accent contrast fix · camera-hang 8s exit w/ ways-out · webview/NotReadable error taxonomy · 3D-load failure surfaced + shimmer disarm · tracker-ready gating of the face hint · SPA links at post-send hop · "Yours" chips on wall tiles/lightbox, device-local only · welcome promoted to front door w/ live moment count + QR retarget gated to db events · experiencesFailed + PickerDrawer honest states · onboarding gestures step + optional challenges step · reduced-motion gates on SendOff/ChallengeCheck · micro-type 7.5-8px → 10px · booth entry-gate ways-out · viewfinder aria); agent-verified tsc 0 · 1990/101 · both builds ✓.

## Open items
- Deploy-time: apply 030 + deploy admin-api (see Next). /admin/landing errors honestly until deployed.
- NOTED from waves: MarqueeGrid tiles lack the "Yours" chip · FeaturedSpotlight builds its own QR target (not retargeted) · WallLightbox Download / ChallengeSelector:156 Trophy / PickerDrawer sparkle still text-noir-900 · PickerDrawer section headers still 8px · /admin/support inbox hardcodes limit 100, no paging · list_promos/list_admins/list_feature_flags/list_catalog unbounded · event delete/rename-slug/archive missing (schema+edge work) · wall-cap meter can vanish on count failure · FrameStudio.tsx orphaned (wire or delete — owner call) · guest keepsakes are localStorage-only (email/SMS capture = schema work) · guest self-delete of posts (design in prior STATE history, needs migration+edge fn).
- OG image/<title> are static in index.html — the CMS states this in its UI; changing them = code deploy.

## Failed attempts
- ATTEMPT 1 [L1] (2026-08-08, guides round 2): added `items-start` to the prompt card grid to stop the clamped prompt stretching -> still painted a 6th line under its own ellipsis; measured clientHeight 117px = 5 lines (89.4) + p-3.5 padding (28), i.e. the clamp was already correct and the BOTTOM PADDING was the reveal window.
- (none this session; env: plain `npm install` dies on onnxruntime postinstall — use --ignore-scripts, reconfirmed.)
