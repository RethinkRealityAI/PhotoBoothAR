## Goal
MERGE STATE (2026-08-02): PR **#35 MERGED to main** (3b35b7b) — Higgsfield provider integration (BYO org keys, real API client, /host/import), Beamwall Frame Forge (live + published on the Higgsfield marketplace), the AR-features booth landing film, and the 12-finding adversarial-audit hardening. This branch (`claude/customer-journey-platform-polish-ejozbj`, PR #36 — customer-journey polish · guest UX · landing CMS · admin health; its migration 030_landing_content + admin-api v16 are ALREADY LIVE) was reconciled with that main and IS ALSO MERGED (34d6e13). Since then: PR #38 week-one social marketing kit merged (6ef1464). FRAME FORGE V2 (2026-08-02, app repo only — no platform files): browser-first mobile rebuild re-measured clean live (2-col 167px tiles, Vault config sheet, 3 tabs, 0 sub-44px targets, all 22 curated presets have real composited example art), audited adversarially (10 findings) with the confirmed fixes (phone error surfacing, retry-uses-submitted-settings snapshot, breakpoint state loss, refresh-vs-paginate, schematic caption) applied and redeployed. CONFLICT RESOLUTIONS in this merge: booth-feature.mp4/poster = the AR film (#36's 7/31 booth recut was superseded by the owner's 8/01 AR-film directive; its wall/challenges/cards recuts ship); Billing.tsx = #36's toast plumbing + #35's Connected-accounts card; remote-assets.json = union (AR clips + hero pngs); beamwall-video.md = union of both learnings appends. NOTE: repo now has TWO migrations numbered 030 (org_provider_keys + landing_content) — both live, do NOT renumber (010_orders precedent); next is 031.
PRIOR: PR #33 studio polish + the baseball-cap first library asset merged at ead18ab; PR #30 (platform redesign) = 85359fa; PR #31 (asset configurator) = f126ef9.

## Now
HIGGSFIELD INTEGRATION (PR #35, draft, CI green, 3 commits 22f7d28→3510943): (1) EDGE — ai-generate-image **v17 DEPLOYED**: real platform-API client (`Authorization: Key id:secret`, model-path submit default `bytedance/seedream/v4/text-to-image` env-overridable, 2.5s poll to 110s cap, nsfw→content_blocked), org BYO key resolved BEFORE the spend (cost **0** on BYO — org pays Higgsfield; `byoKey:true` in ai_jobs input; aiStudio gate still applies), platform key fallback (`HIGGSFIELD_API_KEY` = 'id:secret' split on FIRST colon, or `_KEY_ID`+`_SECRET`), gemini path byte-identical. NEW fns **provider-keys v2** (set/clear/status; secret one-way; ambiguous multi-org refused) + **import-asset v2** (SSRF-guarded https image import → assets bucket + experiences row, `config.transparent` from PNG alpha probe) — both DEPLOYED, deployed source byte-diff-verified vs repo (ai-generate-image differs ONLY in 6 comment dash-run lengths, zero code diff). Migration **030 org_provider_keys** applied live + committed (RLS on, ZERO policies — app_settings is publicly readable, hence the table). (2) CLIENT — provider segment in AiFramePanel/DirectorPanel (localStorage 'bw.aiProvider'), copilot generate_frame proposal.provider + A2UI ChoicePicker, Billing "Connected accounts" card (id:secret paste auto-splits), NEW **/host/import** (handback page: checkerboard transparency preview, event picker, import-asset invoke); frameProcessing now honours `config.transparent===true` (genuine-alpha PNGs skip the chroma-key AND the keyedFraction reject gate — the old gate would have rejected exactly the best inputs); anti-standee clause added to full-scene/duo-scene in BOTH mirrors (verified fix: models painted a photo OF a carnival standee). (3) **BEAMWALL FRAME FORGE** — real Higgsfield app (type:"app", template:"preset"), **LIVE https://beamwall-forge.higgsfield.app + PUBLISHED to the community feed** (marketplace id e7a6badc-c43a-4b32-9ebc-412de681f683): 10 archetypes × 12 themes (incl. wedding lettering monogram/initials/name), browser chroma-key port (Rec.601, histogram key detection), QA gates by archetype, D1 favourites/exports, R2-served handback → `beamwall.netlify.app/host/import?src=…`, 54 tests, live-verified 0 pageerrors/0 overflow both viewports. Gates on the branch: tsc 0 · **1898 tests (97 files)** · build ✓ · legacy build ✓ · 12 platform screenshots clean.
FROM THE CAP-ASSET SHIP (owner's Windows machine, now merged): `planSync.ts:104` pins 'en-US' in `Intl.NumberFormat` (en-CA machines rendered `US$49.00` and broke the test). CAP GLB facts (`public/models/baseball-cap.glb`, 12.1MB): 1 node "Mesh_0" NO rotation, 26,464 verts / 41,141 tris, FRONT = +z, UP = +y, welded components 39 / raw 585; albedo near-greyscale 2048², emissive pure black.
main = f126ef9, plus a1d5e27 where the fetch-remote-assets workflow vendored `public/models/face_landmarker.task` on merge (the cloud sandbox lacks that egress; the workflow has it). Netlify production deploys from main.
NO migrations, NO edge-function changes, NO Supabase deploys were made by either PR — everything the findings implied for the DB is written up in `## Next` for owner review instead. The live DB is unchanged: migrations through 029, edge fns at the versions listed further down.
Gate on the committed tree, always in an isolated worktree with NO `.env.local` so it matches CI exactly: tsc 0 · **1872 tests (96 files)**, from a 1057/71 baseline · build ✓ · `VITE_EVENT=hope-gala` build ✓.
Independent verification (not the waves' own): 21 routes × {390×844, 1440×900} → 0 pageerrors, 0 stub failures; horizontal overflow 0 at 390/360/1440.
LEGACY RENDER-CHECKED, not just build-checked: a `VITE_EVENT=hope-gala` build was served and driven at its TOP-LEVEL legacy routes (`/`, `/booth`, `/wall`, `/challenges`, `/me`) at both viewports — 0 pageerrors, 0 overflow, correct branding and real content. Six waves touched shared code (StageCanvas, FaceRig, Overlay3D, faceTracking, the new lighting), and "the legacy build compiles" is NOT evidence that the legacy site renders.
METHOD THAT MATTERED — gate the COMMIT, not the working tree. The first push went red because a concurrent agent wrote a new file into the gap between the gate run and `git add`. Every push since: commit → `git worktree add --detach <sha>` → symlink node_modules → tsc+vitest+build there → push only if green. That worktree has no `.env.local`, which reproduces the documented CI-only env trap for free.
CONFIGURATOR LIBRARY IS EMPTY BY DESIGN. `LIBRARY_ASSETS` in `src/lib/studio/assetLibrary.ts` ships with only a DEV-only demo entry. A cap GLB WAS generated through Meshy for this work (Higgsfield's `image_to_3d` is Meshy underneath) with configurator-optimal settings — textured + PBR for surface detail, quad topology, symmetry on, 20k polycount, from a deliberately flat unlit source image — but the sandbox network policy 403s CONNECT to both `api.meshy.ai` and the asset CDN, so it could not be downloaded into the repo. The owner has it locally. See `## Next` for the exact handoff.
DEPLOYED EDGE FNS (unchanged by these PRs; from the 2026-07-28 ship): support-api v1 · stripe-checkout v8 · stripe-webhook v10 (verify_jwt OFF preserved — Stripe signs, no JWT) · submit-post v11 · card-publish v9 · card-render v7 · ai-generate-image v15 · ai-generate-3d v8 · admin-api v14 · ai-job-status v9 · ai-event-designer v19.
EDGE-FN WATCHOUT (PR #28 lesson, still binding): tsconfig excludes supabase/ and esbuild only PARSES, so an undefined identifier in an edge fn passes every repo gate — #27's admin-api referenced UUID_RE undeclared and was a guaranteed ReferenceError 500. Before deploying new edge-fn code, bundle with esbuild and grep the bundle for the identifier's definition.

## Next
- **HIGGSFIELD OWNER STEPS (PR #35):** create a key pair at cloud.higgsfield.ai and set `HIGGSFIELD_API_KEY` ('keyId:keySecret') in Supabase secrets — until then the Higgsfield option honestly degrades to ai_not_configured and only BYO-key orgs can use it. **The REST contract is UNVERIFIED against live** (built from official SDK READMEs; defensively parsed; first real generation is the true test — watch the submit-response id field + status-body nesting). Also UNVERIFIED whether platform.higgsfield.ai API keys draw from the consumer credit pool or a separate prepaid API balance — check the cloud.higgsfield.ai billing page.
- HIGGSFIELD FOLLOW-UPS (named, non-blocking): Frame Forge live generate→key→send round trip needs a signed-in human run (agent verified guest surfaces only); 13 of 22 Forge presets render layout schematics (generate their example art in-app); Forge cover lacks the HF glyph (static.higgsfield.ai 403'd from sandbox); Forge tile chips are 32px vs the 44px touch rule (matches shipped template); Copilot FAB overlaps /host/import fine print at 390px (cosmetic); platform fetches Google Fonts at runtime (venue-wifi risk class, pre-existing); no rate limit on provider-keys/import-asset (same open item as validate-challenge-photo); ai-generate-image stores every provider result as .png contentType regardless of actual bytes (pre-existing; import-asset sniffs correctly — pattern ready to port); deployed ai-generate-image differs from repo in 6 comment dash-runs only (redeploy from repo whenever it next ships to close byte-parity).
- ~~**NEXT TASK — add the first real library asset**~~ — DONE and MERGED (branch `claude/first-library-asset`): cap shipped in `LIBRARY_ASSETS` with measured refs (crown 0.2544 · brim 0.2784 · button 0.2050), painted regions (crown 23,884 / brim 2,331 / button 249 verts, spatially audited), on-surface slot. Follow-ups closed: decal unitsPerCm now MESH-LOCAL (was world-space — engraving rendered as giant sheared fragments in every head-anchored view; fixed + tested); `frontAxis` DELETED from the descriptor contract (no render consumer, under-determined for auto-orient); per-region neon emissive verified glowing end-to-end (was already wired); MATERIAL_PRESETS vs FINISHES deliberately NOT unified — FINISHES-as-survivor changes Text3DBuilder's shipped gold/chrome (metalness/roughness deltas + dropped emissives) and porting silver/roseGold into FINISHES would need emissives, violating its metals-have-no-emissive contract that protects an asset's own glow. Pipeline improvements: prep tool measures refLuminance from texels ("Measure from texture", pure `measureRegionLuminances` + tests), anchor raycast-projected onto the mesh surface, `buildTemplateDescriptor` remaps packed bytes when dropping empty regions (positional-contract bug, red-proofed). LIVE test artifacts for the owner: event `cap-test-event` (LIVE, free tier) under test login `beamwall-cap-test@example.com` / `BeamCapTest-2026!` — booth loads the cap from the real DB (glb fetch verified); delete both when done testing.
- STILL UNVERIFIED from that task: the cap rendering on a REAL face in the REAL `/e/cap-test-event/booth` (nobody was in front of the camera when driven; the render path itself is proven in the parity Preview on a live face) — scan the event QR on a phone. Engraving colour defaults to white `#ffffff`; a light-crown cap wants a darker engraving via the picker.
- BEHAVIOUR CHANGES SHIPPED IN #30 that deserve a deliberate look now that they are live: (a) legacy coded events now get the event signature on RECORDED VIDEO, which they never had (photos always did) — the fix for an entitlement hole where free-tier video shipped unbranded, but it does change legacy output; (b) the booth's sparkle layer is drawn from a pre-rendered sprite instead of 46 per-frame gradients — the alpha ramp is reproduced exactly and the sprite is only ever downscaled, so it is visually equivalent but was NOT pixel-A/B'd; (c) `signOut()` now clears the Pro-subscription cache, which previously outlived the session that filled it.
- HIGHEST-VALUE FOLLOW-UP, deliberately NOT built (needs a migration + an edge function, i.e. owner review): guests cannot remove their own post. The obvious design is dead on arrival — `session_id` is returned to anon by `posts_public_read`, so any guest can read every other guest's session id off the wall and an RLS policy keyed on it would let anyone delete anyone. Correct shape: a service-role edge fn taking a per-session HMAC minted by `submit-post` at capture time, rate-limited, audited, deleting the Storage object too — and preferably SOFT delete (`hidden = true`), which is reversible and cannot be weaponised. Also wants a host setting, since some events must not let guests pull content off a live wall mid-event.
- Capture-time DPR boost for the 3D layer (recipe proven, not shipped): `gl.setPixelRatio(n)` + `gl.render()` are both synchronous and the canvas already has `preserveDrawingBuffer`, so a same-task `drawImage` cannot read a cleared buffer — but it needs `StageCanvas.capturePhoto` orchestration, which is inside the "do not touch booth capture" fence. Would recover the remaining 1.45× upscale into the still.
- Named, non-blocking, from this pass: the studio reference bust reads near-white and has lost tonal separation under the new Studio lighting preset (preview-only stand-in); the `neon` finish renders as a flat self-lit silhouette and `glass` reads milky rather than crystal; KTX2/Basis GLBs still fail (honest message, no crash); `glbThumb.captureGlbThumbnail` still uses its own light rig instead of the shared presets; `events.starts_at` is never SELECTed by `list_events`/`overview_metrics` so "events happening today" is absent from the admin triage strip; no `ai_jobs` admin action exists so "AI jobs stuck" is not surfaced; admin payments triage scans only the most recent 50 orders (stated in the UI).
- DECIDED AGAINST, with reasoning, so nobody re-litigates it: bounding `store.fetchPosts`' full reconcile. `posts.length` drives the wall's "N moments" counter, so a limit would silently under-report the event total at exactly the large events where it matters. The reconcile already dropped from 3×/min to ~1×/4min, which makes the original concern moot; doing it correctly needs a separate exact-count query. Also decided against an always-mounted R3F wall background: the wall already mounts a WebGL canvas per arrival and must never hold two at once.
- OWNER-ONLY: set SUPPORT_NOTIFY_EMAIL secret (support-desk email; tickets land in /admin/support regardless — email is never load-bearing). /admin/catalog → "Provision in Stripe" in TEST mode, then again after the LIVE key swap. Stripe LIVE keys (#1 launch gate). Custom SMTP (Resend) per DEPLOYMENT-CHECKLIST §1b. vault.create_secret service_role_key for the ai-jobs-reconcile cron. HaveIBeenPwned toggle.
- First-real-use watches: copilot lettering ask-flow + sample legend on a real generation; validate the new frame archetypes' chroma-key output; support desk end-to-end (file → admin reply → email); admin /admin/features override actually flips a live gate (admin-api v14 is the first version where those actions can work at all).
- Deferred (named, non-blocking): StudioPreview does not pass guest lettering into StageCanvas (studio preview omits the name; booth/photo/video have it); PropertiesDock IA regroup; reorder-invisible; invite-colour accent generalization designed not built; DemoBooth.tsx + stageLayout.ts + adminFilters.ts + Pagination.tsx deletions await owner approval; per-event flag overrides exist in resolver but not on the flags screen (belongs on event detail); guest ticket replies/threading (needs bearer-token subsystem).

## Done
- 2026-07-29 ASSET CONFIGURATOR **STAGE C** (branch `claude/asset-configurator-pipeline`, commits 8194bce + b801557 — NOTE: I did not run git commit/push; something in the environment did) — RESULT: tsc 0 · **1872 tests (96 files)**, from 1797/94 · build ✓ · `VITE_EVENT=hope-gala` build ✓. 18 files, +2473/−89.
  · C1 LIGHT-SWATCH BLOWOUT FIXED — `regionTint.ts` `softShoulder()` + `TINT_SHOULDER_KNEE = 0.9`, a Reinhard shoulder applied to `tint × ratio` before the mix, with an exact GLSL twin (`beamwallShoulder`). Chosen over a filmic curve because it is IDENTITY below the knee, so dark/mid swatches are numerically unchanged. Knee is 0.9 not 0.8 because cream `#e8e2d6` measures **0.80695** on red — 0.8 compressed the swatch itself (caught by the test, not by eye). Cost, stated: `#ffffff` lands at 0.95 linear = sRGB 249 instead of 255. PROOF at scratchpad `stageC/c1-cream-before-after.png` (rendered from the real esbuild-bundled module): same cream bake, **56.4% of pixels pegged at pure white before → 0.0% after**, and the upper topstitch row goes from invisible to fully legible.
  · C2 TEMPLATE TRANSPORT — `SET_MODEL_ASSET` accepted `template?` and NO caller passed it, so the whole feature was dead code. `AssetsDock.configurableTiles` and `useStudioDnd` (DragPayload.template) now carry it. Director/Text3DBuilder/upload paths deliberately pass none — AI/Meshy output has no descriptor.
  · C3 LIBRARY — `src/lib/studio/assetLibrary.ts`, pure data + colocated test, following starterScenes.ts. `LIBRARY_ASSETS` is **EMPTY** and that is the honest state: a configurable asset is a GLB **plus** an authored descriptor, and the only GLB in the repo is the fit-reference bust (a head is not a wearable). `DEMO_LIBRARY_ASSETS` carries it as one DEV-only entry so the path is drivable; `libraryAssets(includeDemo)` filters it out of production. Test enforces the owner's generic-only mandate against a legacy-brand deny-list.
  · C4 CONFIGURATOR UI — Stage B's controls were EDITED-UNVERIFIED; they now render and work (screenshots below). Added the Personalise header + provenance tooltip (badges `preparedBy: 'auto'`), the accented "use each guest's own name" headline card with plain-language copy, and `DebouncedTextInput` (220 ms trailing, flushed on blur AND unmount — decal carve is a measured 45-46 ms main-thread stall).
  · C5 PREP TOOL — `/dev/asset-prep` (`src/dev/AssetPrepTool.tsx`), DEV-gated exactly like StudioHarness; pure maths in `assetPrep.ts` (+37 tests). Previews through the REAL `applyRegionTint` seam, not a lookalike. `preparedBy` is driven by an edit counter.
  · C6 — LETTERING_FONT unified into `letteringFit.ts` (drawn strings pinned byte-identical by test); per-region **emissive implemented** (new `uRegionEmissive`/`uRegionEmissiveAmount`, gated separately from matAmount so gold cannot black out an asset's own glow); `assetTemplate.normalizeCustomization` → `scopeCustomizationToTemplate` and WIRED into draftMapping (it had zero callers).
- 2026-07-29 PLATFORM REDESIGN, six waves (PR #30) — RESULT: tsc 0 · 1641 tests (92 files) · both builds ✓; 21 routes × 2 viewports 0 pageerrors/0 overflow. Method: three read-only Opus audits first (booth/wall/studio), every load-bearing claim re-verified against the code before any brief was written, then waves on DISJOINT file sets so they could run concurrently.
  · BOOTH PIPELINE — the live preview was cropping the frame: the stage used `h-full aspect-[9/16] max-w-full`, but the top bar and control deck are both absolutely positioned so neither consumes flex height, leaving a 0.445 box under a 0.5625 canvas drawn `object-cover`. Measured 98.5px of frame art sliced off at 390×844 and 108px at 430×932. Fixed with the container-query letterbox already shipped in StudioStage. Also: `ShaderRunner.resize()` reallocated the drawing buffer 60×/sec because it assigned width/height unconditionally; `dispose()` never released the context and a fresh 1080×1920 runner was built per shutter press; ~46 CanvasGradients + 2 param objects + 11 uniform lookups + a querySelector per frame, all removed; a gate skips the shader pass when the video frame has not advanced. Recorded video shipped WITHOUT the signature (preview draws `withSignature=false` and video is a captureStream of it) — an entitlement hole, now closed for the recording's duration only.
  · LIVE WALL — the arrival ceremony never showed the photo. `ParticleBeam` (13.7k GPU points, dissolve → arc → pixel-perfect reassembly) already existed but was wired only to the marketing page; the wall now drives it from the real post into the tile's measured rect, falling back to the old beam when pixels are unreadable — proven against a host that genuinely omits CORS headers, after the first attempt was found worthless because Playwright's `route.fulfill` silently re-adds them. Bursts now coalesce instead of every insert overwriting the pending spotlight. REAL BUG: the marquee wrapped with a single `if`, not a modulo, so after tab throttling the huge frame delta overshot by more than one period and the row translated off screen permanently — a blank wall that never recovers. Plus bounded polling, visibility pauses, video-decoder caps, wake lock, viewport-scaled type (2.2× at 4K), and a visible RECONNECTING state.
  · STUDIO — a refresh destroyed an unsaved scene including paid AI generations (no beforeunload, no guard, no autosave); Duplicate was worse, dispatching LOAD which set `dirty:false`, so the copy was unsaved AND unguarded. The layer panel lied: reorder swapped indices in a flat array while the panel rendered three fixed buckets, so "move up" changed paint order with nothing moving on screen. Now one honest flat list with drag-reorder, numeric entry beside every slider, align/snap, and seven zero-credit starter scenes. Drag went from 300 commits to 20 per gesture; the shader loop stopped being torn down on every slider tick.
  · BOOTH GUEST EXPERIENCE — filter orbs now show the live camera through each shader (one shared runner, round-robin, ~7.5 ticks/s) instead of static gradients and emoji; the countdown gained a cancel, a ring and synthesized WebAudio cues; photo strip; gestures; landscape handling; safe areas; and the confirm-modal-before-send replaced with one tap.
  · GUEST KEEPSAKES — bulk save fired N anchor clicks, which iOS blocks after the first, leaving a spinner that never finished. Now native multi-file share where supported, else a hand-written STORE zip — validated externally with `unzip -t` and python `zipfile` on an archive built inside the browser by the shipped code path, not just unit tests (writing the byte-layout test caught a real EOCD size bug). Plus intersection-gated video playback and a client-side recap clip.
  · ADMIN / MANAGER — found a live data-loss bug: `get_wall_settings` discarded its error, so a failed read rendered DEFAULT_WALL_SETTINGS as the event's real values and one tap on Save overwrote a running wall's configuration mid-event. Also one failed-fetch-renders-as-empty regression (`Features.tsx:96`). Manager console rebuilt phone-first with live arrivals, pending counts, keyboard queue and undo.
  · SHARED 3D — one lighting definition (presets as tested data) now used by booth, studio orbit + live, preview, Text3DBuilder and Director cards, so they cannot drift; gold finally reads as metal. drei's `<Environment preset>` was REJECTED because it fetches an HDR from raw.githack.com; the environment is generated locally into a 64px cube target instead. Four GLB loaders collapsed to one shared cache (measured 4 fetches → 1). Draco decoding vendored from the installed three.js. Contact shadows were built and then turned OFF with evidence that nothing in this product has a floor for one to land on.
- 2026-07-25 FINAL BACKLOG PASS (server paging for the last 2 admin lists · client_errors guard+retention · 8 P2 fixes) — RESULT: commit 15b4eb4 + P2 batch; migrations 020/021 applied live and verified by SQL; browser-proven paging/search on both screens; tsc 0 · 781 tests (61 files) · build ✓. admin-api deploy deliberately deferred to branch-ship time (see Next).
- 2026-07-25 AI ORCHESTRATION + ASSET QUALITY + its self-audit — RESULT: 4 commits (6c21ee5 a62f223 90c512e 58ca9d1); 8 self-audit defects found and fixed; tsc 0 · 717 tests (58 files) · build ✓ · legacy build ✓. NOT DEPLOYED (see Next).
- 2026-07-25 PROVIDER RESEARCH (2 subagents, official sources only — ai.google.dev, docs.meshy.ai, developers.googleblog.com are ALL proxy-blocked 403) — RESULT: Gemini findings taken from the live v1beta discovery doc (rev 20260721) + google-gemini/cookbook; Meshy findings from meshy-dev/meshy-mcp-server + meshy-3d-agent (first-party GitHub, second-hand to the docs). Confirmed: NO true-alpha output exists on the Gemini API (ImageConfig has only aspectRatio + imageSize; ImageResponseFormat.mimeType enum is UNSPECIFIED|IMAGE_JPEG), so our green-screen chroma-key is current best practice, not a workaround. Confirmed: Meshy `should_remesh` defaults FALSE, so `target_polycount` was being ignored; `art_style`/`symmetry_mode` are deprecated no-ops; assets are retained only 3 days (we already re-host the GLB in Supabase Storage).
- 2026-07-25 SELF-AUDIT of the UI/UX fixes — RESULT: 10 defects found in my own work, all fixed (commit `fix(audit)`); headline was a shipped regression, `border-radius: inherit` in the global :focus-visible rule squaring every pill button on focus (probed /login 3.35544e+07px→0px). Gates: tsc 0 · 632 tests (52 files) · build ✓ · legacy `VITE_EVENT=hope-gala` build ✓ (first time ever run).
- 2026-07-25 FOUR-SURFACE UI/UX AUDIT + 6 fix packages — RESULT: docs/UI-UX-AUDIT.md + PR #24 (base claude/beta-release-readiness-tihw04). 44-screen sweep 0 pageerrors/0 overflow.
- PRIOR (all merged, detail in git log): studio overhaul 8 waves; InteractiveShowcase landing demo; gate-0 beta readiness (migrations 013-016 live, edge fns submit-post v10 / validate-challenge-photo v2 / ai-event-designer v17 / stripe-webhook v9 / stripe-checkout v7); credits+promo system (migration 011); admin suite.

## Constraints
- Standing (from prior sessions, still binding): never break legacy-events; no new npm deps; tests `.ts` only, never import anything reaching src/lib/supabase.ts; never loosen tenant RLS; liquid-glass idiom on platform surfaces; prefers-reduced-motion; Gemini key never committed; no VITE_EVENT in .env.local during tests; do-not-touch: StageCanvas.drawFrame semantics, booth capture/submitPost, experiences schema; FrameStudio.tsx + DemoBooth.tsx not deleted without owner approval; generic-only template library (no legacy-event branding).
- User (2026-07-30, verbatim gist): "polish and refine anything you think we need to in regards to the customer journey from login to deploying their first experience"; "refine the guest experience ... easy, smooth and amazing"; "admin portal ... especially want to be able to control the landing page via a cms so I can swap out images and videos as needed"; "utilize all necessary subagents"; "feel free to add any wow and amazing features"; "if there's anything for me to do, just note it but you should finish all you can without me".

- User (2026-07-30, landing round 7, verbatim gist): remove the red-marked highlights strips "in all the sections as it shows in the videos already"; "Just a nice one sentence hook then video" — text must not be "repetitive of the videos"; "the video should play automatically as they scroll to that area and it should show the playback controls... there should be play and pause, thats it. even then make it subtle and bottom right of the video"; "check all the videos again ensure they make sense have the best visuals and not redundant and are engaging and hook the user"; header frames get "photorealistic images of various types... by default we should use the ai ones, making sure they match the type for event the frames are for"; "make sure these images are also optimized webp so they load fast. same with any videos"; switchable via the CMS; free rein on other landing improvements — "slick, beautiful and not overly info dense".
- User (2026-07-28, personalization brief, verbatim gist): "for the frames ask users if they want any names or initials on them either if they provide a name for the event or who they're celebrating or a logo then give them options on how it can be implemented like monogram in cursive, bottom or top, within the frame or extending out or do they just want that with no frame. use higgsfield to create some sample images and types of frames covering all these use cases that users can select from in a nice a2ui widget… or from a pill suggestion"; "if you think this flow and thinking is useful for any other parts of the system feel free to implement it too"; "you too think of one thing that would blow the minds of users… and implement it. no need to ask for permission… all permissions are granted"; "verify all rigorously and ui changes with screenshots. like produce assets using these new pathways to ensure all is working and that all tests pass. making sure you always audit your work, checking for any logic gaps and challenge assumptions".

- Wave 7 brief (2026-07-29, admin/operator surfaces, verbatim): "Do NOT commit, push, or open a PR — the orchestrator owns git."; "No new npm dependencies. No migrations. No Supabase deploys. No RLS changes"; "admin-api asserts `is_platform_admin` BEFORE its action switch; do not weaken any authorization path."; "Pure logic in `.ts` + colocated `.test.ts`"; "A test file must NOT import anything whose module graph reaches `src/lib/supabase.ts`"; "Respect `prefers-reduced-motion`."; do-not-edit list incl. `src/lib/db.ts`, `src/lib/auth.ts`, `src/lib/host.ts`, `src/components/booth/**`, `src/components/studio/**`, `src/components/wall/**`.

- STAGE A brief (2026-07-29, asset-configurator engine, branch `claude/asset-configurator-pipeline`, verbatim): "Do NOT commit, push, or open a PR — the orchestrator owns git."; another agent owns STAGE B in `src/types.ts`, `src/lib/studio/state.ts`, `src/lib/studio/draftMapping.ts`, `src/components/Booth.tsx`, `src/components/studio/StudioPreview.tsx`, `src/components/studio/Studio3DView.tsx`, `src/components/studio/PropertiesDock.tsx` — "DO NOT EDIT THOSE."; "No new npm dependencies. No runtime CDN fetches"; "Tests: vitest NODE env, glob `src/**/*.test.ts` ONLY (never `.tsx`), no jsdom/RTL. A test file must NOT import anything whose module graph reaches `src/lib/supabase.ts`."; "Legacy coded events (hope-gala/jenna-jake/detola-wuyi) must be byte-identical when the new fields are absent."; "Respect `prefers-reduced-motion`."; "`src/lib/studio/finish.ts` and `src/components/ar/FaceRig.tsx` — extend, do not rewrite"; "`parts`/`label` ABSENT must be byte-identical to today's behaviour — that is the legacy guarantee."; build privately with `npx vite build --outDir dist-stageA --emptyOutDir`.

- AUDIT-FIX brief (2026-08-01, branch `claude/hicksfield-frame-assets-ud07ol`, verbatim): "Do NOT run git commit/push — the orchestrator owns git."; "do not touch supabase/functions/ai-generate-image or import-asset" (their halves of F1/F3/F7 + the `config.greenScreen` stamp are already fixed by the orchestrator); "Read each target file (or its enclosing regions) before editing, follow existing idioms, keep changes minimal."; "Do NOT rebuild the proposal surface." (F6); "Do NOT touch the meshy path." (F3 sweep).
- STAGE C brief (2026-07-29, configurator UI/library/prep tool, verbatim): "Do NOT commit, push, or open a PR — the orchestrator owns git."; "No new npm dependencies. No runtime CDN fetches. No migration, no schema change, no RLS change."; "Tests: vitest NODE env, glob `src/**/*.test.ts` ONLY (never `.tsx`), no jsdom/RTL. A test file must NOT import anything whose graph reaches `src/lib/supabase.ts`."; "Legacy coded events must stay byte-identical when the new fields are absent."; "Respect `prefers-reduced-motion`."; "keep the maths in the PURE `regionTint.ts` module"; "Respect the existing `sel3D.type === 'model'` gate"; "Reuse the existing control idiom (numeric entry + sliders + chips), do not invent a new visual language. Semantic tokens only."; "Set `preparedBy` honestly: `'auto'` only when no human touched it."; "Library content must be generic — no event-specific branding."
- Stage B brief (2026-07-29, configurator persistence, verbatim): "Do NOT commit, push, or open a PR — the orchestrator owns git."; "DO NOT EDIT THOSE" (assetTemplate.ts / regionTint.ts / assetDecal.ts / finish.ts / FaceRig.tsx — the render stage owns them); "Do NOT change the experiences schema, do NOT add a migration, do NOT touch RLS."; "No new npm dependencies."; "Keep `src/lib/studio/controlSpecs.ts` as the single source of bounds … do not scatter new limits"; "[the `sel3D.type === 'model'`] gate is correct — respect it"; "Legacy coded events (hope-gala/jenna-jake/detola-wuyi) must be byte-identical when the new key is absent"; "Change only what the task requires".

## Decisions
- DECISION: landing CMS = singleton `landing_content` (draft/published jsonb) + SECURITY DEFINER `get_landing_content()` (anon reads PUBLISHED only; revoke-then-grant per the 022 lesson); writes via admin-api (audited) — platform_config was int-only/no-client-policies and an RLS admin-write table has no precedent.
- DECISION: landing media rides the existing public `assets` bucket under `_platform/landing/` — migration 018 already grants platform admins bucket-wide write and `_platform` can never be an event slug; zero new storage policy.
- DECISION: `DEFAULT_LANDING_CONTENT` media fields are undefined = "use bundled import" (Landing.tsx resolves), keeping landingContent.ts pure/test-importable and first paint instant; a total Supabase outage is invisible on the marketing page.
- DECISION: guest QR retarget to /welcome is gated on `RuntimeEvent.source === 'db'` — legacy walls keep byte-identical targets (legacy DOES have /welcome, but the sites are frozen).
- DECISION: Dashboard go-live no longer reloads the page; optional `onStatusChange` prop keeps EventStudio's pill in sync; legacy mounts pass nothing.

## Facts
- Commands: `npm run lint` (tsc) · `npm test` (vitest node, src/**/*.test.ts only) · `npm run build` · legacy `VITE_EVENT=hope-gala npm run build`. Install with `npm install --ignore-scripts`.
- Baseline at session start: tsc 0 · 1966 tests (99 files). After waves: 1990 tests (101 files) per both implementing agents' runs.
- New modules: src/lib/landingContent.ts (pure, +16 tests) · landingContentClient.ts · landingMedia.ts · src/lib/camera.ts classifyCameraError/CameraUnavailableError (+8 tests in camera.test.ts) · src/pages/admin/Landing.tsx (/admin/landing, NAV 'Landing').
- admin-api new actions: get_landing_content_admin / save_landing_draft (≤200k JSON guard) / publish_landing_content / revert_landing_draft — all audited; pre-switch guards untouched.
- store.ts: new `experiencesFailed` flag; PickerDrawer has loading / failed-retry / empty states.
- Playwright: executablePath '/opt/pw-browsers/chromium-1194/chrome-linux/chrome'. Sandbox egress: *.supabase.co and Netlify previews 403 — Supabase only via MCP.
- PR #36 (draft) targets main; Netlify deploy previews build per push.

## Done
- 2026-07-30 THREE AUDITS (host journey 26 findings · guest UX 28 findings · admin+CMS design) — RESULT: ranked implementation briefs, all file:line-cited.
- 2026-07-30 ADMIN HEALTH (commit 60c9f52): Payments renders LoadError instead of confident-empty on failed orders fetch; Credits failure states got Retry.
- 2026-07-30 CMS WAVE — RESULT: migration 030 (committed 2101e0e, NOT yet applied) · admin-api +4 audited actions (NOT yet deployed) · pure normalize layer (hostile-input tests: javascript:/data:/foreign-origin URLs rejected, fixed-slot arrays never resize) · /admin/landing editor (ConfirmModal publish, media slots Replace/Reset, LoadError/useToast, OG-static notice) · Landing.tsx reads content.* with bundled fallbacks; agent-verified tsc 0 · 1990/101 · both builds ✓.
- 2026-07-30 HOST WAVE — RESULT: 12/12 items (ToastProvider on /host + success toasts · Go live + Print signage on create-success · signup resend · live linked getting-started checklist w/ progress ring · Dashboard QR → /welcome + share link, platform-gated · go-live without reload · checkout-return toasts on EventsList/EventStudio · honest wizard steps + disabled-CTA copy · signup password reveal + promo echo · rail New-event CTA + per-path titles + sign-out confirm · platformGuide no longer advertises unmounted FrameStudio · template-seed failure notice); gated in isolated worktree tsc 0 · 1966/99 · both builds ✓.
- 2026-07-30 GUEST WAVE — RESULT: 15/15 items (12-CTA --on-accent contrast fix · camera-hang 8s exit w/ ways-out · webview/NotReadable error taxonomy · 3D-load failure surfaced + shimmer disarm · tracker-ready gating of the face hint · SPA links at post-send hop · "Yours" chips on wall tiles/lightbox, device-local only · welcome promoted to front door w/ live moment count + QR retarget gated to db events · experiencesFailed + PickerDrawer honest states · onboarding gestures step + optional challenges step · reduced-motion gates on SendOff/ChallengeCheck · micro-type 7.5-8px → 10px · booth entry-gate ways-out · viewfinder aria); agent-verified tsc 0 · 1990/101 · both builds ✓.

## Open items
- Deploy-time: apply 030 + deploy admin-api (see Next). /admin/landing errors honestly until deployed.
- NOTED from waves: MarqueeGrid tiles lack the "Yours" chip · FeaturedSpotlight builds its own QR target (not retargeted) · WallLightbox Download / ChallengeSelector:156 Trophy / PickerDrawer sparkle still text-noir-900 · PickerDrawer section headers still 8px · /admin/support inbox hardcodes limit 100, no paging · list_promos/list_admins/list_feature_flags/list_catalog unbounded · event delete/rename-slug/archive missing (schema+edge work) · wall-cap meter can vanish on count failure · FrameStudio.tsx orphaned (wire or delete — owner call) · guest keepsakes are localStorage-only (email/SMS capture = schema work) · guest self-delete of posts (design in prior STATE history, needs migration+edge fn).
- OG image/<title> are static in index.html — the CMS states this in its UI; changing them = code deploy.

## Failed attempts
- (none this session; env: plain `npm install` dies on onnxruntime postinstall — use --ignore-scripts, reconfirmed.)
