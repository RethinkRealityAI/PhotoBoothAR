## Goal
(branch claude/platform-ui-ux-audit-jl5l3k) Audit all four platform surfaces — guest /e/:slug/**, host /host/**, platform admin /admin/**, marketing+auth — for UI/UX defects, then FIX the findings, each package gated. Owner decisions this session (AskUserQuestion): base this branch on PR #23's head (not main) since main is a version behind the live DB; deliver audit AND fixes; cover all four surfaces.

## Now
2026-07-25 (branch claude/platform-ui-ux-audit-jl5l3k, based on claude/beta-release-readiness-tihw04 @ 3898a63 — PR #23's head, NOT main): FOUR-SURFACE UI/UX AUDIT + FIXES. Method: four parallel per-surface code audits against seven fixed lenses (IA, responsive, state honesty, design system, a11y, copy, perceived perf) + a 44-screenshot Playwright sweep (22 routes x 390/1440). Findings ledger committed at docs/UI-UX-AUDIT.md with severity, file:line and fix status.
FIVE PATTERNS behind most findings: (1) eleven data paths caught a failed fetch, returned [], and rendered the EMPTY state — the guest was told "Event not found — double-check the link or QR code", the wall told a full room "be the first to capture a moment", the host was told "No cards yet", the operator "No organizations match"; (2) dead ends at camera-denied and permanent send-failure; (3) ~14 overlays that were not dialogs; (4) load-bearing 8-10px microtype (incl. the pre-camera privacy disclosure); (5) pt-safe-top/pb-safe-bottom used in 7 places and DEFINED NOWHERE.
SIX GATED PACKAGES LANDED (each tsc 0 · vitest green · build ✓; baseline was tsc 0 · 608 tests · build ✓, now 620): 5aea1d0 foundations (safe-area utilities composed via --safe-* custom props so base padding survives — probed 40px; global unlayered :focus-visible ring that also beats existing outline-none; reduced-motion past .animate-float; forced-colors foil fallback; maximum-scale=1 removed) · 6854c47 guest honesty (loadEventConfig returns a discriminated EventLoad, provider gained 'unreachable' + retry + the missing .catch; db.ts result-siblings fetchPostsResult/fetchMyPostsResult/fetchChallengesResult per the existing fetchMyOrgResult convention so NO caller signature changed; store postsFailed/challengesFailed; pure tested lib/listState.ts; wall QR toggle was event-wide-writable from a guest's phone → now per-device, host keeps admin/Settings.tsx:378; projection mode hidden <sm + touch reveal) · 7f5ddd0 guest dead ends + consent · 6290829 host honesty (copilot ticked FAILED actions and flashed success BEFORE executeAction; cards/contributions/ledger/manager-tokens result-siblings; manager create+revoke silent failures; new-event draft caveat + one link shape + QR on phones) · e85cd1c admin money paths (promo toggle failure swallowed; Number('')→0 wrote 0 platform-wide welcome credits and said "Saved."; 5 list screens discarded `error`; credit adjust silent no-op + no confirm; Modal now a real dialog Escape/trap/restore; toasts live-region + errors persist) · da38776 marketing (Essentials sold "Video guestbook" while ENTITLEMENTS.essentials.cardsStandard=false; retention undisclosed on every tier incl. JSON-LD while free=7d; shared tested formatPostCap/formatRetention so landing and UpgradeCard can't drift; unearned "milestone birthdays" proof; privacy clause for marketing use of guest photos — deliberately NOT claiming no-AI-training since guest photos go to Gemini; resendConfirmation path; auth h1s + top-anchored cards).
LOCAL AUDIT RIG (scratchpad only, NOT in the repo, nothing shipped): a Node http Supabase stub (GoTrue + PostgREST-with-eq/in-filters + admin-api fixtures + a /__fail?tables= fault injector) on :54999 + .env.local pointing VITE_SUPABASE_URL at it (never sets the legacy event var) + Playwright driving vite dev. This is how /host and /admin were rendered at all. Playwright note: repo pins playwright 1.61 (chromium-1228) but the image ships 1194 → launch with executablePath:'/opt/pw-browsers/chromium-1194/chrome-linux/chrome'.
BEHAVIOURALLY PROBED (not reasoned about): with failures injected, wall/challenges/my-photos render "Couldn't load … Try again" and a failing event lookup renders "Can't reach this event — your link is fine", while with the fault cleared the genuinely-empty wall still renders "Be the first to capture a moment"; the admin dialog moves focus inside, closes on Escape and returns focus to its trigger; Customers renders "Couldn't load customers … Nothing below is a complete list" instead of "No organizations match"; Tab produces a 2px accent ring on / and /login.
STILL OPEN (in docs/UI-UX-AUDIT.md with file:line): /host/events/:id/* mounted OUTSIDE HostLayout so the sidebar vanishes in the studio; StudioShell unsaved-work guard + silent duplicate-fork on failed load; ~14 overlays still not routed through the fixed Modal; sub-44px tap targets across booth/wall/host/admin; admin lists fetch every row with client-side filtering; wall ships full-resolution originals to phones. RESOLVED 2026-07-25: draft/ended guest gating is now IN (src/lib/eventAccess.ts + EventContext). The blocker was never the gate, it was that hosts test drafts THROUGH the guest booth (the copilot's test_experience hands them the real guest URL) — so the rule keys on status AND org membership: guests are turned away, members get the booth plus a "Preview — guests can't open this yet" banner. Unknown statuses fail CLOSED. A live event skips the membership round-trip entirely, so the common guest path is unchanged in speed. Legacy coded events hard-code status:'live' (runtime.ts codeRuntimeEvent) so the three frozen sites bypass it.
SELF-AUDIT (2026-07-25, same branch): audited my own six packages — 10 defects, all fixed. Worst was mine: `border-radius: inherit` in the new global :focus-visible rule REPLACES the focused element's radius (it does not shape an outline), squaring every pill button on keyboard focus. Also fixed: 4 audited P0 swallows the packages missed (EventStudio moderation queue — a failed read rendered NOTHING because the queue only draws when pending.length>0, so guest photos silently never went live; CopilotPanel rows??[]; Concierge snapshot catch; legacy admin Moderation), 3 window.confirm calls against the repo's own documented idiom (AssetsDock.tsx:184) now a shared ConfirmModal, a per-device QR override that was not persisted (projector lost it on refresh), 4 landing bullets silently dropped, formatPostCap now tier-aware (the cap counts POSTS and paid tiers include video; free.videoEnabled=false so it must not say "& videos"), a new --on-accent luminance token so accent CTAs stay readable, and failure-path tests (proven red-then-green).

POLISH PASS (2026-07-25, same branch, after the self-audit): owner brief — make it beautiful and not overwhelming; liquid-glass with 3D depth, bevels and drop shadows so surfaces float; same treatment in the AI chat; haptics on press; floating nav (pill on mobile, detached rail on desktop); rebuild the guest booth around the landing demo's full-screen camera UI. Owner decisions: legacy sites ADOPT the new look in their own colours (token-driven); desktop nav = floating glass rail.
Landed: (1) index.css design-system primitives — `.liquid-glass-raised` (two-shadow elevation: tight contact + wide ambient, over a hairline bevel), `.liquid-glass-inset` (recessed), `.pressable` (scale + shadow collapse), `.sheen`; all derived from --accent-rgb/--color-accent so every event theme drives them. (2) lib/haptics.ts (+5 tests) — short 8-20ms patterns; NOTE navigator.vibrate is unsupported on iOS Safari, so haptics are an enhancement over .pressable, never the only feedback. (3) BOOTH REBUILT: new booth/BoothTopBar.tsx (floating glass circles top-right; flip/hide + a "more" menu collapsing Wall/Photos/Upload/Share; router Links so no camera re-permission) + booth/BoothControlDeck.tsx (category tabs → one orb row → shutter; mode toggle + timer folded into the tab row; gradient scrim not a panel) + pure lib/boothDeck.ts (+16 tests). FilterOrbs is now UNUSED. Shutter defined ONCE and shared by the deck and the chrome-hidden view (was duplicated and drifting). (4) Floating nav: HostLayout + AdminLayout are inset floating rails; GuestNav pill raised; admin rail now scrolls (fixed the audit P1 where 9 items clipped at 390px). (5) AI chat: inset composer with focus glow, pressable+haptic send AND Enter, raised user bubbles vs recessed assistant bubbles, floating A2UI cards, three-dot typing indicator.
PROVEN IN A BROWSER (not reasoned): /e/hope-gala booth shutter glow = rgba(212,175,55) gold vs platform rgba(91,140,255) blue — the legacy-in-own-colours decision; booth at 390/1440 with a fake camera, 0 pageerrors, first tab 12px / first orb 14px; .pressable transitions transform+box-shadow+filter normally and ONLY filter under reduced motion; admin rail scrollWidth 536 vs client 372 with "Sign out" reachable. Gates: tsc 0 · 653 tests (54 files) · build ✓ · VITE_EVENT=hope-gala build ✓ · 44-screen sweep 0 errors/0 overflow.
NOT DONE (honest): FilterOrbs.tsx and PickerDrawer's "All filters" sheet still carry the OLD visual language — the sheet is reachable from the deck's "···" orb, so it is now a style island. Wall, cards and upload surfaces did not get the new glass treatment.

AI ORCHESTRATION + ASSET QUALITY PASS (2026-07-25, same branch, after the polish pass): owner brief — the AI agent's orchestration when creating assets; frames "not the most creative or interesting and seem very basic"; 3D props "not rendering correctly, especially like mask, because they are not concave or hollow, with holes in the right place", wrong size, missing detail; the agent should ASK the user for anything a widget needs before rendering/spending; "feel free to add any other features"; then audit challenging all assumptions. Plus: research Gemini + Meshy docs with subagents to use them fully.
ROOT CAUSE was prompt-level, not model-level, and confirmed at file:line — a frame prompt was chroma-key mechanics plus `Design brief: <what the host typed>` with NO composition, motif, material or quality language; the 3D concept prompt never said the object was WORN, so image→3D fused a face into the mesh ("solid mass, no hole where the head goes").
Landed across 4 commits (6c21ee5 · a62f223 · 90c512e · 58ca9d1): (1) `src/lib/assetPrompt.ts` (+tests) — per-kind wearable geometry (mask · helmet · hat · crown · glasses · ears · held · generic), each stating what is hollow, where it opens, wall thickness in mm, real-world cm scale and the camera angle that reveals the cavity; `paletteDirection`; `buildFrameArtDirection` (register per event type, disciplined palette, composition, craft, quality bar, no-text ban). MIRRORED into `ai-generate-image` (buildPrompt/artDirectionFor) and `ai-generate-3d` (withWearability) — edge fns cannot import from src/, so change one, change the other. (2) `src/lib/assetBrief.ts` (+tests) — gap detection before spending. (3) NEW `src/lib/proposalGaps.ts` (+20 tests) — per-tool required fields mirroring `normalizeActions`, plus the brief-quality bar; CopilotChat's confirm handler now ASKS instead of acting, keeping the card mounted. REQUIRED gaps block every press; QUALITY gaps (a real but vague brief) are asked once then the host is believed. (4) Gemini: `responseModalities ['TEXT','IMAGE']`, `imageConfig.imageSize '2K'` (default 1K was BELOW our 1080×1920 buffer), `aspectRatio` on EVERY call (9:16 border / 1:1 sticker — before, only green-screen frames set it, so a reference image chose the aspect), HTTP-200 safety refusals (`finishReason`/`promptFeedback.blockReason`) now `content_blocked` (NOT retryable — every retry path re-runs the same refused words), "no green on the artwork" anti-spill wording, and ANY non-key 400 falls back to the exact shape running in production. (5) Meshy: text→3D now chains preview→**refine** (preview is GEOMETRY ONLY — bare grey mesh), falling back to shipping the preview whenever refine can't start or fails; `experiences.config.textured` records which. (6) `nameHint` on ai-generate-image so a concept image is named after the brief, not after its own geometry paragraph.
SELF-AUDIT of that work found 8 defects, ALL FIXED, 2 of them serious: `frameBriefGaps('a gold frame')` returned NO gaps (MOTIF listed `frame`/`border` — the noun of the artefact), and my first Gemini fallback only fired when Google's 400 prose named the field while still sending TEXT+IMAGE, so an unsupported knob could have taken image generation down platform-wide. Also fixed: `stagedProgress` halved progress for single-stage image→3D (bar stuck at 50%); helmet was handed the mask rules (eye holes vs a neck opening); the `Geometry:` marker let a host disable the wearability rules by typing it; `events.config.accent` does not exist (palette is `accentHexes`); the concept-image Library name; and the 3D panel stopped polling while telling the host the model "will appear in your Library shortly" (nothing polls ai_jobs — no cron, no resume-on-mount — so it would not have; there is now a Keep waiting button).
PROVEN, NOT REASONED: the confirm gate replayed over 11 real proposal shapes (blank title blocks both presses · "a gold frame" asks once then proceeds · a specific brief never asks · `12/09/2026` blocked, `2026-09-12` passes) and `buildConceptPrompt('a chrome helmet')` checked at runtime for the neck opening and the absence of eye holes. Gates: tsc 0 · 717 tests (58 files) · build ✓ · `VITE_EVENT=hope-gala` build ✓ · all 4 edge fns parse (esbuild).

BACKLOG-CLEARING PASS (2026-07-25, same branch, after the AI pass): owner brief — "build it and f8x anything the audit found even if its outside this task. we wna the okatfrm to be fully polished and ready for release". Closed the whole P1 backlog plus the ai_jobs gap found while auditing.
Landed (a032a95 · 84f2244 · a273f38): (1) NEW `src/lib/aiJobs.ts` (+15 tests) + `useAiJobSweep.ts` — mount-time recovery for Meshy jobs nobody is watching; mounted in HostLayout AND EventStudio (the studio sits OUTSIDE HostLayout). KEY INSIGHT, do not "simplify" this away: gating on `updated_at` DOES NOT WORK because ai-job-status writes nothing while a task is PENDING/IN_PROGRESS, so a job under active polling looks stale — MIN_AGE_MS (11 min, just over AiGeneratePanel's poll budget) is the only honest signal. (2) `getExperienceResult` sibling in db.ts — a failed read used to open the blank starter draft and saving FORKED a duplicate experience; StudioShell now refuses to edit. (3) StudioShell dirty guard on the back arrow, primary action = Save (handleSave now returns boolean so save-and-leave cannot navigate away from a failed save); ConfirmModal gained `extraAction` for the third choice. (4) EventStudio credit pill → Billing (the one part of the platform rail that was information, not chrome; the rail itself stays out per the owner's "less encumbered" call). (5) admin-api `list_orgs`/`list_events`/`list_orders` now search + page SERVER-side (they had NO limit at all); `paging()`/`likeTerm()`/`page()` helpers, `hasMore` via a +1 sentinel row; pure tested `serverList.ts` + `useServerList.ts` (a response for a term the operator already changed is DISCARDED); `ListMore.tsx` replaces the numbered pager. (6) `mediaUrl.ts` (+16 tests) + `PostImage.tsx` — wall/gallery tiles request a Supabase resize URL sized to their display width, falling back to the original on the FIRST error because image transformation is a paid plan feature unverifiable from here. (7) NEW `src/lib/useDialog.ts` — Modal's behaviour extracted; 9 overlays are now real dialogs (3 via Modal, 6 keep their own shape: booth sheet, manager drawer, 2 viewers, upload confirm, studio tour). `enabled` flag because several render conditionally inside an always-mounted parent. (8) Tap targets: MEASURED at 390px across 9 routes — was 5 on the wall / 27 on /host / 12 on /admin/events, now 0 everywhere.
PROVEN IN A BROWSER (rig at :5173 + stub at :54999, seeded with one 45-min-old and one 60-second-old running Meshy job): the sweep polled EXACTLY `job-abandoned-1` and left `job-young-1` alone, 0 page errors, and the stub logged the poll arriving. UpgradeCard + host QR dialogs: role+aria-modal present, focus inside, Escape closes, focus restored, 0 page errors. Tap-target probe: 0 under 44px on all 9 routes. 18-screen desktop sweep: 0 overflow, 0 page errors (4 console 404s were the stub having no ai_jobs table — which incidentally proved fetchRunningAiJobs's failure path renders clean).
Gates (no `.env.local`, matching CI): tsc 0 · 759 tests (60 files) · build ✓ · `VITE_EVENT=hope-gala` build ✓.

## Next
DEPLOYED 2026-07-25 (owner said go): ai-generate-image **v14**, ai-generate-3d **v7**, ai-job-status **v7**, ai-event-designer **v18** — all ACTIVE, repo and prod back in lockstep.
FIRST-RUN WATCH (nothing about these could be tested from the sandbox — *.googleapis.com and api.meshy.ai are both proxy-blocked, so "it compiles" was the only proof available). On the first real generation, check the edge logs for:
- `gemini rejected the generation config — falling back` → gemini-2.5-flash-image will not take imageSize 2K and/or responseModalities TEXT+IMAGE. Output still works (it retries with the exact pre-deploy config), but frames stay at 1K: either move frames to gemini-3-pro-image or drop GEMINI_IMAGE_SIZE.
- `refine rejected` / `refine unavailable — shipping the preview mesh` → the Meshy refine body needs adjusting (it retries once without enable_pbr). Text→3D still returns a mesh, just untextured, and `experiences.config.textured` records which.
- `gemini_image_safety` / `content_blocked` responses → the new HTTP-200 refusal path is firing (previously these were misreported as generation_failed).
PR #24 (draft, base claude/beta-release-readiness-tihw04) carries the audit + 6 fix packages + the self-audit fixes; watched, CI green expected. Awaiting owner: the draft/ended guest-gating decision (hosts test drafts THROUGH the guest booth via the copilot test_experience flow, so gating would break a shipped path); then the P1 backlog at the end of docs/UI-UX-AUDIT.md, led by /host/events/:id/* being mounted outside HostLayout and StudioShell's missing unsaved-work guard.

## Done
- 2026-07-25 AI ORCHESTRATION + ASSET QUALITY + its self-audit — RESULT: 4 commits (6c21ee5 a62f223 90c512e 58ca9d1); 8 self-audit defects found and fixed; tsc 0 · 717 tests (58 files) · build ✓ · legacy build ✓. NOT DEPLOYED (see Next).
- 2026-07-25 PROVIDER RESEARCH (2 subagents, official sources only — ai.google.dev, docs.meshy.ai, developers.googleblog.com are ALL proxy-blocked 403) — RESULT: Gemini findings taken from the live v1beta discovery doc (rev 20260721) + google-gemini/cookbook; Meshy findings from meshy-dev/meshy-mcp-server + meshy-3d-agent (first-party GitHub, second-hand to the docs). Confirmed: NO true-alpha output exists on the Gemini API (ImageConfig has only aspectRatio + imageSize; ImageResponseFormat.mimeType enum is UNSPECIFIED|IMAGE_JPEG), so our green-screen chroma-key is current best practice, not a workaround. Confirmed: Meshy `should_remesh` defaults FALSE, so `target_polycount` was being ignored; `art_style`/`symmetry_mode` are deprecated no-ops; assets are retained only 3 days (we already re-host the GLB in Supabase Storage).
- 2026-07-25 SELF-AUDIT of the UI/UX fixes — RESULT: 10 defects found in my own work, all fixed (commit `fix(audit)`); headline was a shipped regression, `border-radius: inherit` in the global :focus-visible rule squaring every pill button on focus (probed /login 3.35544e+07px→0px). Gates: tsc 0 · 632 tests (52 files) · build ✓ · legacy `VITE_EVENT=hope-gala` build ✓ (first time ever run).
- 2026-07-25 FOUR-SURFACE UI/UX AUDIT + 6 fix packages — RESULT: docs/UI-UX-AUDIT.md + PR #24 (base claude/beta-release-readiness-tihw04). 44-screen sweep 0 pageerrors/0 overflow.
- PRIOR (all merged, detail in git log): studio overhaul 8 waves; InteractiveShowcase landing demo; gate-0 beta readiness (migrations 013-016 live, edge fns submit-post v10 / validate-challenge-photo v2 / ai-event-designer v17 / stripe-webhook v9 / stripe-checkout v7); credits+promo system (migration 011); admin suite.

## Constraints
- User (2026-07-21, post-gate-0, verbatim gist): "I have set up the resend settings already. can you make sure all our emails are beautiful, and on brand" — set up in Supabase via MCP if possible, else provide copy-paste HTML; "audit your implementation, ensure all tests have been created and passed, challenge any assumptions and feel free to polish anything else as a nice to have before release".
- User (2026-07-21, landing round 6, gist): hero — remove the redundant free-to-try text, "Maybe keep the pill, and remove the other text, whichever one you think is best to keep"; section decor images — "too small... alternating corners: left for the first one, right for the second... make those images bigger and strategically placed"; wall/frames decor — "I don't like that frames image. Let's just get maybe one or two singular frames with no backgrounds, nicely placed so that they [blend] with the page and nicely poke out"; booth decor — "make that image of the young black male. I think we already have that in our Higgsfield account, so look for it. If not, generate it... as if they're almost interacting with the video themselves"; films — "appropriate headers in the scenes... flow a bit better", fix booth selfie "not properly filling the camera frame. There's a weird gap", "transitions from scene to scene more interesting where appropriate", "text bigger, especially on mobile", "overall video bigger and easier to read", "I like the content... as a good baseline... refine it so you don't have to do anything from scratch"; "set sub-agents appropriately to revise each one, with you overseeing everything and then visually confirming"; promo film — "remove the final video and just archive it... give it to me later so I can use it for promo on socials"; replace that section with "Who this platform is for AND THE TYPE OF EVENTS IT'S FOR... Event planners and organizers... conferences, trade shows/conventions" with "great graphics using Higgsfield"; scroll — "as we scroll to each section, the video component should tilt, which they kind of do on desktop but not on mobile... video plays as it tilts"; mobile hero carousel — "like the desktop, where we have the one big frame and then the smaller ones on either side. Right now, it's just two by two"; "Still add the 3D tilt effect there"; "Make sure everything is smooth"; free rein for extra polish/wow.
- User (2026-07-20, landing round 5, verbatim gist): "free should be highlted mroe on the landign apge, make the demo butotn better and mroe prominent"; "immersive virtual photobooth should be highlighted"; "add arrows to the side of the carousel to allow people to scroll"; "lvie moments from real events needs ot be mroe prominent"; "reduce the space from the hero to the steps"; "make the steps a bit mroe clear, ahrd to read the numebrs, and say step 1, .. etc"; sections: "keep the titles, and make the descriptions succint, and all the info be in the respective hey gen videos, especial yth ebullet points"; "remove it [girl selfie video] from the live photo wall one"; "for each of the hey gen videos... use images instead of balnk frames or anyhting so it's mroe beleivable"; layout = "jsut the section icon, highlight title, header title, succient one liner, then video underenaht not side by side... scrool animation that they titl back a bit almost liek a screen... big liek the full expereicne video"; "remove the images from overllaping on the videos... make them be behind the videos and pop up from the corders in a 3d manner and scroll into view... angle and title them... small but noticable so they don't distract"; "make sure each of the seciton videos make sense and no redudant info, especiall ythe keepsake one"; "verify all outputs with screenshots"; "Ask any quesiton for clairty, no asusmptions". AskUserQuestion locked: video imagery = Higgsfield-generated; bullet info = animated text callouts (videos are muted); section video size = full-experience (max-w-4xl).
- User (2026-07-18, landing round 4, from screenshots): demo 3D "Neon Shades" reads as two disconnected pink blocks ("double 3d props") — fix the material/geometry so it reads as glasses, not a compositing bug (Overlay3D+StageCanvas dual-draw architecture matches the real Booth and is correct); demo wall container has a "weird black" hard edge — soften it, and widen the wall a bit; "Capture again" button → bg-foil gradient like the rest of the site; remove "Pick a style, in one tap" section, fold its copy into the Immersive-booth feature section; hero carousel — remove the plain-text label under each frame AND the baked event-name text inside 4 of 6 frame SVGs (swap to de-branded/unflagged twins, verified text-free), confirm auto-scroll actually runs; move hero title/tagline up so the carousel is more visible; shrink the "massive gap" in the header nav and make it more liquid-glass (`.liquid-glass` utility); How-it-works step images bigger + staggered scroll-in animation per step, more dynamic.
- User (2026-07-18, landing round 3): title = "Your Immersive Virtual Photobooth"; remove "AR" from the hero paragraph ("magical is fine" = keep); darker overlay on the background so beams don't fight text and frames pop; fix the black border around hero frames; hero carousel = 3D coverflow (center card bigger/taller, scales down as it scrolls past) + smooth auto-scroll on mobile; demo: remove the pick-a-frame part, remove the numbered steps block (paragraph covers it), remove the external "Open camera" button — make the on-phone CTA prominent/enticing instead; how-it-works step images regenerated via Higgsfield.
- User (2026-07-17, verbatim): "the navigation is too busy and should maybe be a side panel, especially on larger devices and especially when you go to studio mode that's less encumbered"; "The studio nav can be cleaned up… so things aren't overlapped or hidden behind any other secondary toggles like the undo. The name of the experience should also be much larger"; "consolidate some settings or things that we try to manage with individual nav links into one settings link"; "Make sure you are also testing and taking screen shots to verify things look good and are responsive"; mobile: "make it as best as possible".
- User (2026-07-17, verbatim): "Make sure that things are generated properly and the intent is explicitly followed from the user's prompts in any of the ways we access our agents" (reported bug: asked general agent for a 3D model, got a sticker).
- User (2026-07-17, verbatim): "if we do generate stickers or images, the background is also being removed from them"; "We still need to fix the sizing of 3D models. They are way too small"; "the current head of the 3D mode with the attachment points is still too small compared to a human head, so we need to make it twice as big by default."
- User (2026-07-17, verbatim): "make sure we remove all the hard-coded templates, like the SCAGO gala or any that have specific branding for any of the legacy events. We're just on generic frames and assets that will be part of the template library that all users will have access to." (Remove from the self-serve template library only — legacy-events sites themselves must keep working.)
- User (2026-07-17): first-run host journey = agentic-first, "not overwhelming or adding to the bandwidth, just easy to set up a new event. They can just share the QR code immediately" — experiences are optional after that.
- User (2026-07-07): Gemini API key must NEVER be committed to the repo; Supabase edge-function secrets only.
- (platform) Never break legacy-events sites — faceRig/Booth/Overlay3D are shared; keep default behavior compatible.
- (platform) No `.env.local` with VITE_EVENT during tests.
- (task) No new npm dependencies; edge fn dir needs its own deno.json; never loosen tenant RLS.
- (plan, user-approved) Do NOT touch: StageCanvas.drawFrame semantics/Transform2D meaning, booth capture/submitPost, RLS/migrations, experiences schema (studio persistence = jsonb config + app_settings 'studio' key only).
- User (2026-07-10): "an experience doesn't have to be just 2D or 3D... It's both" — mixed scenes are the default model; content persists across 2D/3D/Preview view switches.
- User (2026-07-10, W7 verbatim): "we just have a section called My Assets and then maybe different sections: Studio Library... 3D Generated or Generated Assets... Uploaded Assets"; Director "Not overly conversational"; rejection "should have a flow for capturing the intent again for that rejected asset"; reference uploads "Use this image to generate a frame" / "Use this image to generate a 3D model".
- AskUserQuestion (2026-07-10 W7, locked): asset click = add instantly + settings expand below tile; trigger actions = ALL THREE (particle bursts, piece reveal, filter pulse); Director regen after reject = charged, clearly labeled (frame 1cr, 3D 11cr).
- User (2026-07-10, InteractiveShowcase): "While I like the frame, the UI of the camera, everything, and also maintaining our colors" — keep the current in-camera UI idiom, frame, and brand colors; amplify, don't replace. "sticking to the general premise of what the below mini PRD for this component is" (idle|camera|beaming|wall, phone collapse, beam, polaroid drop, capture-again). "Remove the old one for this new effect" — no doubled photo in flight; particles are photo-sampled PLUS a sprinkle of spectrum hues.

## Decisions
- DECISION (2026-07-25): a REQUIRED gap blocks every confirm press; a QUALITY gap (a real but vague brief) is asked ONCE and then the host is believed. Asking twice is arguing, not helping — and a host who wants "a gold frame" is allowed to have one.
- DECISION (2026-07-25): every new provider knob must degrade, never fail. Gemini falls back to the exact generationConfig running in production on any non-key 400; Meshy refine falls back to shipping the untextured preview. Neither is testable from the sandbox, so the fallback IS the safety net.
- DECISION (2026-07-25): edge-fn deployment of AI money-path changes is the owner's call, not automatic — the providers are unreachable from here, so "it compiles" is the only thing the sandbox can prove.
- DECISION (user, 2026-07-10, W6): Scene Director folds into the Platform Copilot — one assistant, studio-aware, docked right-side panel (not modal); accepted assets dispatch into the open draft; composer cards per asset (checkerboard 2D, R3F 3D viewer, Meshy progress); scene templates; booth reveal animation.
- DECISION (user, 2026-07-10, W5): Meshy ONLY for runtime 3D gen; Gemini concept image → ai-generate-3d image mode for head pieces; frame transparency = browser-side chroma-key (no new npm deps).
- DECISION (user, 2026-07-10): MIXED SCENES — one experience = ≤1 frame + N stickers + N 3D pieces + one optional scene-level filter slot; soft cap 20 (frame exempt); kind 'composite' when mixed; single-family scenes keep today's kinds.
- DECISION: occluder from vendored MediaPipe canonical_face_model.obj (metric cm = faceRig.ts anchor space) + procedural cranium ellipsoid; colorWrite:false, renderOrder −1, raycast no-op → composites in the booth with zero StageCanvas changes.
- DECISION (W8): headScale calibration gets a live tracker-fit estimate + Apply chip storing a baseline; the booth then transfers per-guest fit by RATIO to that baseline (opt-in, zero change without a stored baseline). ReferenceBust is GLB-only — the procedural fallback head was deleted per user instruction ("make sure the old 3D one doesn't show at all").
- DECISION: hand-rolled pointer-event DnD (no dnd-kit) — portal DragGhost + setPointerCapture; pure drop math in lib/studio/dnd.ts.
- DECISION (2026-07-10, InteractiveShowcase): ShowcasePhone/CameraExperience runs the REAL booth pipeline (StageCanvas + Overlay3D), not a mock — the landing demo has full booth fidelity; BeamStrike/ParticleBeam are pure WAAPI/WebGL presentation with zero booth-state coupling; cross-device transport = Supabase Realtime broadcast (ephemeral, no auth/DB writes) with a BroadcastChannel test twin (`?beamlocal=1`, `L`-prefixed ids); DemoBooth.tsx is superseded and unreferenced but intentionally NOT deleted pending explicit user approval.

## Facts
- MIRROR PAIRS (edge fns cannot import from src/ — change one, change the other): `src/lib/assetPrompt.ts` KIND_SPEC/COMMON_3D ↔ `ai-generate-3d` KIND_GEOMETRY/withWearability; `assetPrompt.ts` buildFrameArtDirection/paletteDirection/EVENT_REGISTER ↔ `ai-generate-image` artDirectionFor/paletteDirection/EVENT_REGISTER. `buildMeshyPrompt` is the canonical tested spec and is NOT called from the client on purpose (ai-job-status names the experience from the RAW brief via nameFromPrompt, truncated to 40 chars, so an enriched prompt would name a piece after a geometry paragraph).
- Edge fns have no type gate (tsconfig excludes supabase/) and no deno in this image. Syntax gate that works: `npx esbuild supabase/functions/<fn>/index.ts --format=esm --outfile=/dev/null`. NOTE `--loader=ts` only applies to stdin — passing it makes esbuild error out, which is easy to mistake for a pass if the command is `|| true`-ed.
- events.config palette = `accentHexes: string[]` (ordered, [0] dominant; buildRuntimeConfig at src/events/runtime.ts:106, written by src/lib/eventTemplates.ts:220). There is NO `config.accent` — the single accent colour is `BrandingOverrides.colors.accent` in the app_settings 'branding' value, keyed by SLUG (db.ts:693).
- Meshy text→3D is TWO tasks: `mode:'preview'` = geometry only (untextured), then `mode:'refine'` + `preview_task_id` textures it. Image→3D is ONE task with `should_texture:true`. `should_remesh` defaults FALSE, so `target_polycount` needs it explicitly. Provider assets expire after 3 days.
- Supabase MCP edge-fn deploy: pass `import_map_path:"deno.json"` or the API mangles the prior absolute path.
- Branch: claude/platform-ui-ux-audit-jl5l3k, based on origin/claude/beta-release-readiness-tihw04 (PR #23 head 3898a63), NOT main — PR for it must target that branch, not main. Baseline on that base: tsc 0 · 608 tests (49 files) · build ✓; after this session's six packages: 620 tests (51 files).
- node_modules must be installed with `npm install --ignore-scripts` (plain install dies on onnxruntime-node's postinstall CDN fetch).
- Sandbox network: *.supabase.co AND the Netlify deploy preview both fail with `CONNECT tunnel failed, 403` through the egress proxy — no live data, no deploy-preview verification from here.
- Local audit rig (scratchpad, outside the repo): rig/supabase-stub.mjs on :54999 (GoTrue + PostgREST with eq/in filters + admin-api fixtures + `GET /__fail?tables=posts,events,admin-api` fault injection) + .env.local pointing VITE_SUPABASE_URL at it + rig/shoot.mjs (44-screen sweep) and rig/probe-*.mjs.
- Playwright: repo pins 1.61 (expects chromium-1228) but the image ships 1194 → always pass executablePath:'/opt/pw-browsers/chromium-1194/chrome-linux/chrome'.
- Safe-area utilities are defined in src/index.css and COMPOSE with a base padding via `--safe-top`/`--safe-bottom` custom properties (`class="pt-safe-top [--safe-top:0.75rem]"`) — a bare `padding: env(...)` wins the cascade over the Tailwind class beside it and would zero the design padding on non-notch devices.
- The global `:focus-visible` rule is UNLAYERED on purpose, so it beats Tailwind's utilities layer including existing `outline-none`/`focus:outline-none`.
- List reads that back an empty state use the `*Result` sibling convention (fetchMyOrgResult → fetchPostsResult/fetchMyPostsResult/fetchChallengesResult/listCardsResult/listContributionsResult/fetchLedgerResult/listManagerTokensResult), returning `{rows, failed}`; the render decision is the pure tested `listState()` in src/lib/listState.ts.
- Branch: claude/studio-editing-ux-improvements-xfn36m (merging PR #15 in, then → main). Commands: npm run lint (tsc --noEmit) · npm test (vitest run, node env, src/**/*.test.ts only, never .tsx) · npm run build · dev :5173 (`vite --host`; CLAUDE.md's ":5180" is stale).
- tsconfig excludes supabase/ — edge fns not type-checked by lint.
- Camera hook: src/components/booth/useCameraStream.ts `useCameraStream(enabled, withAudio)`; booth video id booth-video; studio video id studio-video (ONE camera, shared across 2D/3D/Preview).
- Head space: src/lib/faceRig.ts ANCHOR_PRESETS in cm (crown y+8.3, ears ±7.7, chin −9.4, noseTip z+7.6); RIG_CAMERA origin fov 63; One-Euro filtered pose (OneEuroVec3/OneEuroQuat); live head-fit ring-buffer estimator (getHeadFitEstimate, median of ~45 samples) feeds W8 auto head-size.
- Persistence: src/lib/db.ts experiences config jsonb; getStudioSettings/setStudioSettings (app_settings key 'studio', additive {headScale, baselineFit?, autoHeadScale?}); uploadAsset→assets bucket. Deep links ?id= from Library.tsx.
- Vendoring: scripts/remote-assets.json + .github/workflows/fetch-remote-assets.yml. canonical_face_model.obj (468v, bbox verified) + public/models/reference-head.glb (Higgsfield bust, GLB-only render — see Decisions).
- Design tokens: src/index.css @theme (liquid-glass = premium standard, --accent-rgb); lucide-react + BeamIcons gradient idiom; motion/react; zustand; react-resizable-panels; ui/Tooltip.tsx (portal, delay).
- GEMINI_API_KEY is VALID in prod (verified 2026-07-21: 5 `ai_credit_spends` rows in the last 7d, latest 2026-07-17; user confirmed). The old "REJECTED / 400 API_KEY_INVALID" note was a stale 2026-07-08 snapshot — purged from CLAUDE.md same day. ai_key_invalid detection in edge fns stays as honest resilience, not an active condition.
- GEMINI TRAP: ARRAY-of-OBJECT responseSchema hangs gemini-2.5-flash constrained decoding — encode arrays as JSON STRING fields (e.g. actionsJson, planJson patterns).
- shaders.ts: ShaderParam/ShaderDef, SHADERS, SHADER_MAP, FILTER_SHADERS, class ShaderRunner (incl. aspectCropped cover-crop fix).
- StageCanvas drawFrame steps: 1 video mirror · 2 shader coverFit · 3 three canvas drawImage · 4 overlay transform · 5 sparkles/trigger-fx (additive-optional) · 6 signature. Buffer: FIXED 720×1280 preview + object-cover, capture path 1080 — canvas buffer immune to the CSS-transform getBoundingClientRect measurement trap.
- borders.ts pure: BUILTIN_BORDERS, toDataUrl, BuiltinBorder{id,name,kind,svg}. cn() at src/lib/cn.ts.
- Edge fn pattern: supabase/functions/*/index.ts (json(), serviceClient(), user JWT auth); ai-generate-image v12, ai-event-designer v12 deployed (studio Director: reference images, reply+optional plan).
- eventTemplates.ts (EVENT_TEMPLATES, templateConfigPatch); host.ts createEvent/updateEventConfig.
- DemoBooth.tsx (822L, superseded/unreferenced — see Decisions): DEMO_FRAME_SVGS, FRAMES/FILTERS/PROPS, BeamFlightFx, Orb/OrbThumb, capture/measureFlight/beamToWall/wall grid.
- Landing.tsx: FEATURES, FilmEmbed, GSAP scroll choreography, demo section now mounts InteractiveShowcase.


## Open items
- SUPABASE ADVISORS, triaged 2026-07-25. FIXED: the assets-bucket cross-tenant hole (migration 018). NOT FIXED, with reasons: (a) 8 `rls_enabled_no_policy` INFOs on admin_audit / ai_designer_usage / guest_quota / orders / platform_config / promo_codes / promo_redemptions / stripe_webhook_events are CORRECT BY DESIGN — RLS on with no policy = deny-all to clients, service role bypasses; (b) 14 `*_security_definer_function_executable` WARNs on is_org_member / event_org / event_org_by_id / is_event_member / is_platform_admin / event_is_public / card_event — these are the helpers RLS policies call, so revoking EXECUTE would BREAK every policy; the real remediation is moving them to a non-exposed schema, which means rewriting every policy that references them on a live DB with no way to integration-test from this sandbox. Leak is small (an org uuid for a public slug; admin-status for a uuid you already have). (c) `client_errors_insert` WITH CHECK (true) is the anonymous error sink — intentional, but unbounded; wants a size/shape guard. (d) Leaked-password protection is OFF — a dashboard toggle only the owner can flip (Auth → Passwords → HaveIBeenPwned).
- ai_jobs RECOVERY IS NOW BOTH client-side (`useAiJobSweep`, covers a returning host) AND scheduled (`ai-job-status` sweep mode + pg_cron `ai-jobs-reconcile`, every 15 min, migration 019 — covers a host who never returns). ONE-TIME OWNER STEP REMAINING: the cron reads the service-role key from Vault, so it is a no-op until someone runs `select vault.create_secret('<SERVICE_ROLE_KEY>', 'service_role_key');`. Until then the client-side sweep still covers returning hosts and nothing is broken; check `cron.job_run_details` after setting it.
- ADMIN LISTS: `list_users` still caps at 1000 (auth.admin.listUsers page 1) and `list_audit` at 200, both un-paged — they were not the audit finding but they are the same cliff further out. Users/Audit therefore still filter client-side (and still use `adminFilters.ts` + `Pagination.tsx`, which is why those files are not dead).
- WALL IMAGE RESIZING IS UNVERIFIED IN PRODUCTION: Supabase image transformation is a paid plan feature and the sandbox cannot reach the project. `PostImage` falls back to the original on the first error, so the worst case is one wasted request per image and today's behaviour. Confirm on the real project (look for `/storage/v1/render/image/public/` 200s) and, if it is not enabled, either enable it or delete the transform path.
- The 44-screen sweep last ran at ONE viewport (18 desktop screens, 0 overflow / 0 page errors). The 390px half of that sweep has not been re-run since the tap-target and dialog changes; the dedicated tap probe covers 9 routes at 390px.
- MESHY REFINE DOUBLE-CHARGE RACE (known, bounded, documented at the call site): two polls inside the same ~1s window (two studio tabs on one job) both create a refine task and we pay Meshy for one wasted one. Claiming before the call would need a sentinel provider_job_id that a concurrent poll would GET as a 404 and refund a healthy job. Correctness (no double-materialize, no double-refund) is guarded; only cost leaks.
- MESHY PARAMS RESEARCH SAYS ARE UNUSED and deliberately NOT adopted (second-hand sources, unverifiable from the sandbox, and a rejected body would break 3D generation outright): `ai_model:'meshy-6'`, `model_type:'lowpoly'`, `origin_at`, `auto_size`, `resize_height` (via /openapi/v1/remesh — would give METRIC scaling, the real fix for prop size), `pose_mode`, `hd_texture`, `texture_image_url`, SSE streaming at `/<endpoint>/{id}/stream` (could replace polling). `art_style` is a deprecated no-op but KEPT: removing an accepted param is riskier than sending a dead one.
- GEMINI OPTIONS NOT ADOPTED: `gemini-3-pro-image` for frames (thinking model, best at graphic composition, up to 14 reference images vs 3 on flash) — a model swap needs a real side-by-side, which the sandbox cannot do; multi-reference prompting (style-ref + palette-ref, weighted in prose since no weighting param exists); instruction-driven editing to repair a bad frame centre without re-generating ("repaint the central region flat #00FF00, keep the border art pixel-identical").
- TEST TRAP (cost one red suite this session): a colocated `.test.ts` must not import any module whose graph reaches `src/lib/supabase.ts` — `createClient` throws without VITE_SUPABASE_URL/ANON_KEY, and CI (`npm ci --ignore-scripts` → `npm run lint` → `npm run test -- --run` → `npm run build`) has no env file, so the test file fails to COLLECT ("Test Files 1 failed", 0 tests run). This is why the plan table lives in the pure `src/lib/plans.ts` and `src/lib/entitlements.ts` is only the React hook re-exporting it. Never verify a suite with a rig `.env.local` present.
- Live-hardware checklist (studio, needs camera + valid keys): transparent frame generation; Director ideation→plan→dwell-previews→Add-N; reject+feedback regen; reference image→frame/→3D; Meshy piece lands ~14cm + gizmo drags 1:1; My Assets inline settings; Magic Triggers firing bursts/reveals/pulses live AND in the captured photo; tracker-estimate Apply → booth per-guest transfer.
- Live-hardware checklist (landing): scan the `/beam/:channelId` QR on a real phone against the deploy/production URL to confirm the Supabase Realtime wire end-to-end (unverifiable from sandbox — *.supabase.co blocked).
- DemoBooth.tsx is superseded + unreferenced — NOT deleted, pending explicit user approval to delete.
- Minor deferred (studio, all logged non-blocking): gizmo can scroll off-frame on deep orbit zoom (cosmetic); triggers on a filter-only scene don't fire in the booth yet; revealedIds not reset on photo retake; Mine-tab fetch failures read as empty (pre-existing db.ts swallow); mobile top-bar buttons are icon-only; CLAUDE.md dev-port line says :5180 (real :5173).
- Platform launch gates: Stripe LIVE keys; custom SMTP (Resend) wiring in Supabase dashboard (Gemini key is valid — no longer a gate).

## Failed attempts
- ATTEMPT 1 [L1] (env, npm install): plain `npm install` → `read ECONNRESET` during reify/postinstall.
- ATTEMPT 2 [L1]: immediate retry → same ECONNRESET, log pins it at `postinstall:node_modules/onnxruntime-node` (external binary CDN reset by proxy; registry fetches fine — use `npm install --ignore-scripts`, not needed for lint/test/build).
