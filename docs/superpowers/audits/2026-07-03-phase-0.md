# Phase 0 Audit — Prerequisites & Infra Protection

**Date:** 2026-07-03 · **Branch:** `claude/photo-wall-saas-strategy-xd1rxf`

## Actions completed

1. **PR #4 merged into `main`** (squash, `20594aa`) — guest Upload-to-Wall flow. Working
   branch synced with main (clean merge, no conflicts).
2. **`legacy-events` branch created** at `20594aa` (main tip incl. PR #4) — the freeze
   point for the three live event sites.
3. **Mechanical gate on merged branch:** tsc clean · 35/35 vitest pass · vite build OK.
4. **Netlify enumeration** (team `rethinkrealityai`): the three legacy sites are
   - `galabooth` (site 2cb4d780-ef89-4434-ae51-265fca8f47f6) — Hope Gala
   - `jennajake` (site bb29cbc3-90d5-479b-96b9-c24c0cfadd39) — Jenna & Jake
   - `theadetoyis` (site 56a31eff-cb9a-427b-b08c-875d0a74732a) — Detola & Wuyi
5. **Supabase pre-flight** (project `zrtftliozslrjomxbfrr`, SCAGO-HopeGala-PhotoBooth):
   - Tables: `posts` (92 rows: hope-gala 59, detola-wuyi 32, jenna-jake 1),
     `experiences` (7), `challenges` (12), `app_settings` (4). All RLS-enabled.
   - **Policy snapshot (rollback reference):** each table has ONE permissive policy
     `<table>_all` — `cmd ALL, roles {public}, USING (true), WITH CHECK (true)`.
     Storage: `posts_objects_all` / `assets_objects_all` on `storage.objects` —
     `ALL, {public}`, qual/check `bucket_id = '<bucket>'`. Buckets `posts`, `assets`
     both `public=true`.

## Deviations & containment

- **Netlify MCP cannot change a site's production branch** (no such operation) and
  **Netlify write operations require interactive approval** not available in this run
  (`create-new-project` for `beamwall` was blocked). Containment:
  - The rebuild risk only materializes when new platform code reaches `main` — i.e.
    when PR #5 merges. Until then, legacy sites rebuild only with code identical to
    what they run now.
  - **REQUIRED USER ACTIONS before merging PR #5** (each ~30s in the Netlify UI, or
    approve the MCP calls in a live session):
    1. `galabooth` → Site configuration → Build & deploy → Branches → Production
       branch = `legacy-events`
    2. Same for `jennajake` and `theadetoyis`.
    3. Create the `beamwall` site (team rethinkrealityai), link repo
       `RethinkRealityAI/PhotoBoothAR`, production branch `main`, build
       `npm run build`, publish `dist`, env: `VITE_SUPABASE_URL`,
       `VITE_SUPABASE_ANON_KEY` (same values as legacy sites), **no `VITE_EVENT`**.
  - This checklist is repeated in the PR #5 description as a pre-merge gate.

## Audit 0 verdict

- Legacy sites: all three serving current deploys (state `ready` at enumeration).
- Freeze point exists (`legacy-events` @ `20594aa`); pinning is a documented pre-merge
  user action (MCP-blocked), so the protection invariant "a main push cannot change
  legacy sites after PR #5 merges" is enforced by the pre-merge checklist.
- DB rollback reference captured above. Migrations not yet applied (Phase 1).
