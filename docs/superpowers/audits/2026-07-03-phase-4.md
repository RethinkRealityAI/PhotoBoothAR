# Phase 4 Audit — AI Generation (image + 3D)

**Date:** 2026-07-03 · **Branch:** `claude/photo-wall-saas-strategy-xd1rxf`

## Delivered (authored + committed; DEPLOYMENT DEFERRED to resume — see below)

- **`ai-generate-image`** — JWT-authenticated, org-membership + server-side
  aiStudio entitlement checks (free tier → 403 upgrade_required), credits
  spent FIRST via `spend_credits` (1cr Gemini / 2cr Higgsfield, 402 on
  insufficient) with guaranteed refund via `grant_credits` on any post-spend
  failure. Gemini = `gemini-2.5-flash-image:generateContent` server-side with
  `GEMINI_API_KEY` secret; Higgsfield = fully typed scaffold behind
  `HIGGSFIELD_API_KEY` (503 + refund until keys exist). PNG alpha probed via
  IHDR; output stored `assets/{slug}/ai/{jobId}.png` → unpublished
  `experiences` draft (source ai_gemini/ai_higgsfield) + `ai_jobs` record.
- **`ai-generate-3d`** — same auth/entitlement/credit pattern (10cr). Meshy
  text-to-3d v2 (`mode:'preview'`, triangle topology) or image-to-3d v1
  (`should_texture`, `target_polycount` clamped ≤50k — polycount guard moved
  into the request rather than post-hoc decimation). Async: returns the
  running `ai_jobs` row.
- **`ai-job-status`** — member-checked polling; on Meshy SUCCEEDED the GLB is
  re-hosted into our `assets` bucket and an unpublished `3d_attachment`
  experience is created with the exact booth config shape
  (`anchor:{anchor:'crown', offset, rotation, scale}` — matches
  `src/lib/faceRig.ts`; the spec's 'headTop' name didn't exist and was
  corrected). Terminal transitions claim the row with
  `.eq('status','running')` so concurrent polls cannot double-create the
  experience or double-refund.
- **Client:** `src/lib/ai.ts`; Creator2D's browser Gemini call REMOVED —
  `VITE_GEMINI_API_KEY` deleted from client/env (key becomes the
  `GEMINI_API_KEY` function secret); Creator3D gains the AI panel with 5s
  polling; Library empty-state entry point; error mapping for 402/403/503
  with balance display.
- **Deferred Phase 3 item landed:** `submit-post` now enforces the plan-tier
  post cap in `handleInit` (free 25 / essentials 500 / premium+deluxe
  unlimited; active org Pro subscription lifts it; the three legacy slugs
  exempt) — checked before the rate-limit bump so capped events don't burn
  guest quota.

## Gate

tsc clean · 50/50 vitest · vite build OK · `VITE_EVENT=hope-gala` smoke OK.

## Audit findings & dispositions

1. Credit-safety review: spend-first + refund-on-every-failure paths traced
   through both functions; job rows always reach a terminal status; refund
   uses the same ref for reconciliation. PASS.
2. Legacy grandfathering verified server-side: legacy slugs exempt from both
   the AI entitlement gate and the post cap (their seeded rows are
   plan_tier 'free', which would otherwise have broken them). PASS.
3. Key-leak check: no `VITE_GEMINI` references remain in src/ or .env.example;
   AI keys exist only as function secrets. (Bundle grep re-run at resume as
   part of deployment.)
4. Behavior change to document: legacy `/admin` AI generation now requires a
   signed-in Legacy-org member + the server key — the passcode alone no
   longer grants generation (the browser key is gone). Listed in the
   user-facing notes.

## Deployment checklist for RESUME (functions are keyless-inert until secrets exist)

1. Deploy `ai-generate-image`, `ai-generate-3d`, `ai-job-status` — verify_jwt
   ON, `import_map_path: 'deno.json'`.
2. REDEPLOY `submit-post` (v3 — post-cap change is committed but the live
   function is still v2).
3. Set secrets: `GEMINI_API_KEY` (required for image gen), later
   `MESHY_API_KEY`, `HIGGSFIELD_API_KEY`/`HIGGSFIELD_API_URL`.
4. Post-deploy: live Gemini generation end-to-end + dist bundle key grep +
   credit decrement/refund probes (Audit 4 protocol).
